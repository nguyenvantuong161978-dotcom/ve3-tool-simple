1.0.677
2026-04-04 (VN)
Fix: IPv6 Pool route ::/0 KHONG update khi rotate → TAT CA worker mat mang
- BUG: Worker rotate (403) → Pool burn subnet cu → xoa gateway tren MikroTik
  → Nhung route ::/0 tren Windows VAN tro toi gateway cu (da bi xoa)
  → _add_to_interface() thay _default_route_gateway da co → SKIP them route moi
  → TAT CA IPv6 traffic timeout (ke ca worker khac)
- FIX: Detect gateway thay doi trong _add_to_interface()
  → Gateway moi != gateway cu → xoa route cu + them route moi
  → Update _default_route_gateway → cac worker khac dung route moi
  → Traffic IPv6 tiep tuc hoat dong sau rotation
- File: modules/proxy_providers/ipv6_provider.py

1.0.676
2026-04-04 (VN)
Fix: Server GUI crash khi Start - px_keys_text widget bi destroy
- BUG: _get_proxy_config() LUON goi _get_px_keys() du mode khong phai ProxyXoay
  → Neu widget bi destroy → TclError: invalid command name
- FIX: Them try/except trong _get_px_keys() va _update_px_key_count()
  → Tra ve [] neu widget khong con → khong crash
- File: server/server_gui.py

1.0.675
2026-04-04 (VN)
Debug: ProxyXoay log exit IP that (IP Google thay) sau setup va rotate
- Proxy server endpoint co dinh (160.250.166.13:11622)
- Exit IP (IP Google thay) thay doi moi lan rotate nhung KHONG duoc log
- Them _log_exit_ip(): Goi httpbin.org/ip qua proxy de lay exit IP that
- Log khi setup lan dau + sau moi lan rotate
- Giup debug 403: biet IP co thay doi that su hay khong
- File: modules/proxy_providers/proxyxoay_provider.py

1.0.674
2026-04-04 (VN)
Fix: Mode SMALL project xong 100% anh nhung KHONG copy ve master
- BUG: video_mode setter normalize "small" thanh "full"
  → GUI set video_mode = "small"
  → setter: "basic" in "small" = False → default "full"
  → settings.yaml luu video_mode: full (SAI!)
  → get_project_status() doc video_mode = "full" → doi video xong
  → Mode Small KHONG tao video → KHONG BAO GIO "done" → KHONG copy
- FIX: Them "small" vao video_mode setter (truoc "basic" check)
  → "small" → "small", "basic" → "basic", con lai → "full"
- File: vm_manager.py

1.0.672
2026-04-04 (VN)
Feature: Server GUI - Them ProxyXoay vao giao dien cai dat
- Them option "ProxyXoay" vao combobox che do proxy
- UI nhap API keys (moi dong 1 key), chon socks5/http
- Nut TEST: kiem tra ket noi key dau tien
- VALIDATE khi START: so key PHAI >= so Chrome
  → Thieu key → hien thong bao loi, KHONG cho chay
  → Moi Chrome can 1 key rieng de co IP khac nhau
- Save/load ProxyXoay settings (px_keys, px_type)
- Pass proxyxoay config vao _get_proxy_config()
- File: server/server_gui.py

1.0.671
2026-04-04 (VN)
Fix: Them ensure_proxy_alive(min_ttl=420) cho 5 video methods bi thieu
- 5 methods goi truc tiep tu smart_engine, BYPASS generate_video()
  → generate_video_chrome(), generate_video_force_mode()
  → generate_video_t2v_mode(), generate_video_pure_t2v()
  → generate_video_modify_mode()
- Khong co TTL check → proxy die giua luc tao video → mat 6 phut cho timeout
- FIX: Them TTL check (min 420s = 7 phut) truoc retry loop
  → Proxy TTL thap → rotate truoc → video tao tren proxy moi
- File: modules/drission_flow_api.py

1.0.670
2026-04-04 (VN)
Fix: ProxyXoay - auto-whitelist IP + socks5h + log nha mang
- Auto-whitelist: Detect IPv4 public → gui kem moi API call
  → VM moi tu dong duoc whitelist, khong can config tay
- test_connectivity(): Dung socks5h:// (DNS qua proxy) thay vi socks5://
  → Fix loi "connection forcibly closed" khi test
- Log them nha mang + vi tri tu API response (vd: [vnpt/ThaiNguyen1])
- DA TEST THUC TE: Chrome Portable qua proxy → IP khac may ✓
- File: modules/proxy_providers/proxyxoay_provider.py

1.0.669
2026-04-04 (VN)
Fix: claim() treo vinh vien do Google Sheets API khong co timeout
- VAN DE: claim() goi _get_account_from_sheet() qua gspread API
  → gspread KHONG co default timeout
  → Mang cham hoac Google API khong phan hoi → treo vinh vien
  → VM khong claim duoc ma → khong lay duoc project tu master
- FIX: _call_with_timeout_retry() - timeout + retry 3 lan
  → account: max 15s/lan × 3 retry
  → topic: max 10s/lan × 3 retry
  → character: max 10s/lan × 3 retry
  → Reset sheet cache giua cac lan retry (load lai connection moi)
  → Sau 3 lan fail → claim VAN TIEN HANH (chi thieu metadata)
- Files: modules/robust_copy.py

1.0.668
2026-04-04 (VN)
Fix: Excel Worker khong lay ma tu master - diagnostic logging + auto-detect path
- VAN DE: Sau "Scanning for projects..." khong co log [QUEUE] nao
  → master_projects path sai hoac khong ton tai → skip toan bo
  → VM khong lay duoc ma moi du master co du lieu
- FIX 1: Diagnostic logging truoc khi vao TaskQueue
  → Log RO RANG ly do skip: path not set, path not accessible, ongoing project
  → Neu path sai → list folders trong auto_path de debug
- FIX 2: Auto-detect alternative master paths
  → Thu "ve3-tool-simple/PROJECTS" va "PROJECTS" truc tiep
  → Tu dong chuyen sang path dung neu tim thay
- FIX 3: scan_available() log chi tiet
  → Log moi project bi skip va ly do (no SRT, in visual, claimed)
  → Log tong so dirs scanned va available
- Files: run_excel_api.py, modules/robust_copy.py

1.0.667
2026-04-03 (VN)
Fix: SMB mat ket noi sau VM restart - tu dong reconnect
- VAN DE: VM restart → mat SMB credential → Z:\AUTO khong truy cap duoc
  → Project xong khong copy ve master duoc (treo)
  → VM khong co gi moi de lam (khong doc duoc master)
- FIX: ensure_smb_connected() tu dong reconnect 2 cach:
  → CACH 1: Detect mapping cu tu 'net use' → reconnect voi password
  → CACH 2: Neu khong co mapping → thu 3 IP may chu (88.254, 88.14, 88.100)
  → cmdkey luu credential → Windows khong hoi lai password sau restart
  → get_working_auto_path(): Thu reconnect khi path mat ket noi
  → run_excel_api.py: Re-detect auto_path moi scan cycle
  → vm_manager.py: Re-detect khi project done ma auto_path mat
- Config: settings.yaml > smb > servers de them/doi IP may chu
- Files: modules/robust_copy.py, run_excel_api.py, vm_manager.py

1.0.665
2026-04-02 (VN)
Fix: IPv6 Pool rotate_all() gay mat ket noi RDP - them delay giua cac lenh router
- VAN DE: rotate_all() gui 300+ lenh lien tuc (150 xoa + 150 them) toi MikroTik
  → Router CPU qua tai → routing table khong on dinh
  → TAT CA IPv6 traffic bi anh huong (ke ca 100 IP YouTube khong lien quan)
  → RDP qua IPv6 bi cham/mat ket noi khi Pool chay
- FIX: Them time.sleep(0.1) giua moi lenh add/remove tren router
  → Router co thoi gian xu ly xong truoc khi nhan lenh tiep
  → 300 lenh × 0.1s = ~30s tong cong (chap nhan duoc cho rotate_all)
  → IPv6 traffic khac (RDP, YouTube) khong bi gian doan
- File: ipv6/ipv6_pool.py

1.0.664
2026-04-02 (VN)
Fix: IPv6 Pool bao ve 100 IP YouTube - HANG RAO CUNG khong cho Pool dong vao
- mikrotik_api.py: Reserved subnet range 01-65 (100 IP YouTube)
  → _is_reserved_subnet(): Check subnet co reserved khong
  → _check_address_reserved(): Block add IPv6 vao reserved subnet
  → add_ipv6_address(): Guard truoc khi add → return None neu reserved
- ipv6_pool.py: Guard o 3 cho:
  → burn_ip(): KHONG xoa gateway reserved subnet tren router
  → rotate_all(): Skip reserved subnet khi xoa IP cu
  → _refill(): Skip reserved subnet khi tao gateway moi
- BAT KE config subnet_start la bao nhieu, reserved subnet KHONG BAO GIO bi dong vao
- Files: mikrotik_api.py, ipv6_pool.py

1.0.663
2026-04-02 (VN)
Feature: TTL-aware proxy - phan biet proxy die vs 403 that (GENERIC, khong rieng ProxyXoay)
- THIET KE GENERIC: Dung interface has_ttl()/get_ttl()/ensure_proxy_alive()
  → drission_flow_api.py KHONG biet provider cu the la gi
  → Chi check has_ttl() → ap dung logic TTL chung
  → De xoa/them provider moi sau nay (chi can override has_ttl/get_ttl)
- base_provider.py: Them 3 methods vao ProxyProvider ABC:
  → has_ttl() → False (mac dinh, IPv6/Webshare khong co TTL)
  → get_ttl() → 9999 (mac dinh, luon song)
  → ensure_proxy_alive(min_ttl) → rotate neu TTL thap
- proxyxoay_provider.py: Override has_ttl()=True, get_ttl() tu _proxy_expire_time
- PHAN BIET 2 LOAI LOI (cho provider co TTL):
  → Proxy die (TTL<=10s): _recover_dead_proxy() rotate + restart, KHONG dem 403
  → 403 that (TTL>10s): dem counter, 3 lan → doi IP + fingerprint
- _recover_dead_proxy(): Check TTL truoc MOI error handler
  → 403, timeout, network error → deu check proxy die truoc
  → Neu proxy die → recover ngay, khong dem 403
- TTL AUTO-ROTATE truoc khi tao anh/video:
  → Image: min 120s TTL | Video: min 420s TTL
- Files: base_provider.py, proxyxoay_provider.py, drission_flow_api.py

1.0.662
2026-04-02 (VN)
Fix: Firewall block IPv4 chan ProxyXoay - xoa rules cu khi dung proxy IPv4
- VAN DE: May truoc dung IPv6 → firewall block IPv4 cho Chrome
  → Chuyen sang ProxyXoay (IPv4 proxy) → Chrome khong ket noi duoc
  → Login load trang Google nhung khong render (email/password not found)
- FIX: Xoa firewall rules khi dung proxy IPv4 (ProxyXoay/Webshare)
  → google_login.py: Goi _unblock_ipv4_for_chrome_static() truoc login
  → drission_flow_api.py: Goi _unblock khi setup Chrome voi proxy
  → Them _unblock_ipv4_for_chrome_static() method
- Files: google_login.py, drission_flow_api.py

1.0.661
2026-04-02 (VN)
Fix: ProxyXoay login + pool_api override bug
- google_login.py: Them proxyxoay vao get_proxy_arg_from_settings()
  → Login qua ProxyXoay proxy (truoc: chi ho tro IPv6/Webshare)
- __init__.py: Fix pool_api_url override khi da chon webshare/proxyxoay
  → Truoc: co pool_url → luon chon IPv6 (ke ca khi type=proxyxoay)
  → Sau: chi override khi type la ipv6 hoac none
- Files: google_login.py, __init__.py

1.0.660
2026-04-02 (VN)
Feature: Them ProxyXoay.shop proxy provider - proxy xoay Viet Nam
- MOI: modules/proxy_providers/proxyxoay_provider.py
  → Ket noi proxyxoay.shop API (SOCKS5/HTTP)
  → Moi worker dung API key rieng (api_keys list)
  → Tu dong doi cooldown 60s khi rotate
  → Chrome ket noi direct (khong can bridge)
- modules/proxy_providers/__init__.py: Them proxyxoay vao factory
- server/chrome_pool.py: Them proxyxoay vao setup_proxy_providers()
- vm_manager_gui.py: Them GUI chon ProxyXoay (main + settings popup)
  → Nhap nhieu API key (moi dong 1 key)
  → Chon proxy type (socks5/http)
  → Nut TEST kiem tra ket noi
- Files: proxyxoay_provider.py, __init__.py, chrome_pool.py, vm_manager_gui.py

1.0.659
2026-04-01 (VN)
Fix: Login cho mang IPv6 cham - tang thoi gian cho + PHAI login thanh cong
- google_login.py: Tang max wait 30s → 60s cho Google redirect (IPv6 cham)
- _run_chrome1.py: Retry 10 lan moi Chrome, doi 10s giua cac lan
  → Chrome 1 PHAI login OK moi tiep tuc (khong bo qua)
  → Chrome 2 fail thi van tiep tuc voi Chrome 1
- Files: google_login.py, _run_chrome1.py

1.0.657
2026-04-01 (VN)
Fix: Login check thong minh - doi URL thay doi thay vi sleep co dinh
- Truoc: sleep 10s co dinh → mang nhanh thi lang phi, mang cham thi thieu
- Sau: Loop 1s/lan (max 30s) doi URL roi khoi trang login
  → Nhanh khi mang nhanh (2-3s), cho du khi mang cham (30s)
  → Khi URL thay doi → cho them 5s de page on dinh
  → Log moi 5s de biet dang cho
- File: google_login.py

1.0.655
2026-04-01 (VN)
Fix: Login retry + verify - khong tao anh khi chua login thanh cong
- VAN DE 1: Login FAIL (OTP khong duoc accept) nhung code van tiep tuc
  → _run_chrome1.py KHONG check return value cua login_google_chrome()
  → In "Chrome 1 login done!" du login FAIL
  → DrissionFlowAPI mo Chrome MOI (chua login) → vao project URL → bi out
- VAN DE 2: Khi login FAIL, driver.quit() tat Chrome
  → DrissionFlowAPI setup tao Chrome MOI tren cung port → khong co session
- FIX 1: _run_chrome1.py retry login toi da 2 lan cho MOI Chrome
  → Check return value cua login_google_chrome()
  → Neu CA 2 Chrome login FAIL → DUNG LAI, khong tao anh
  → Tranh lang phi thoi gian voi Chrome chua login
- FIX 2: google_login.py them VERIFY sau login thanh cong
  → Navigate myaccount.google.com de xac nhan login that su OK
  → Neu redirect ve accounts.google.com → login gia → return False
  → Giong cach server/chrome_session.py _check_current_account()
- Files: google_login.py, _run_chrome1.py

1.0.653
2026-04-01 (VN)
Fix: Server login dung SAI port + SAI IPv6 → Chrome conflict + pha mang
- VAN DE 1: Login dung port rieng (self.port + 100 = 19322)
  → Mo Chrome RIENG tren port 19322, worker chay port 19222
  → Sau login, worker khong connect duoc Chrome (sai port) → "browser connection fails"
  → Chrome zombie chay port 19322 chiem profile folder
- FIX 1: Login dung CUNG port voi worker (self.port - 9222 = worker_id)
  → Chrome da quit truoc khi login → port trong
  → Login mo Chrome tren cung port → setup() connect ngay
- VAN DE 2: Login dung get_proxy_arg_from_settings() → doc ipv6.txt (he thong cu)
  → Server Chrome dung Pool IPv6 (ProxyProvider) → login dung IPv6 KHAC
  → Login setup IPv6 moi + tao 49 default routes → pha mang server
- FIX 2: Login dung CUNG proxy voi worker (tu ProxyProvider)
  → Uu tien self._proxy_provider.get_chrome_arg()
  → Fallback: get_proxy_arg_from_settings() chi khi khong co ProxyProvider
- File: server/chrome_session.py

1.0.652
2026-04-01 (VN)
Fix: IPv6 Pool tu phuc hoi - burned subnets quay lai available sau cooldown
- VAN DE: Server Chrome bi 403 lien tuc → burn_ip() xoa subnet → tao subnet moi
  → Subnet moi cung bi 403 → burn tiep → het subnet → reuse subnet vua burn → loop
  → Pool can kiet IPv6, server khong tao duoc anh
- FIX: Auto-recover burned subnets sau cooldown (max 10 phut)
  → _burned_subnets: Dict[subnet, burned_at] - track subnet + timestamp
  → _auto_recover_subnets(): Khi subnet da qua cooldown → tao gateway moi + IP moi → available
  → Goi tu get_ip() va burn_ip() (truoc khi chon subnet moi)
  → Pool LUON co du IPv6 vi subnet tu phuc hoi sau 10 phut
- FLOW:
  → Subnet bi 403 → burn → tracked voi timestamp → doi 10 phut
  → Sau 10 phut → auto-recover: tao gateway moi + random IP → available
  → Pool tu can bang: burn rate ≤ recover rate → luon co IPv6
- Persist: burned_subnets luu vao pool.json, survived restart
- File: ipv6/ipv6_pool.py

1.0.651
2026-04-01 (VN)
Fix: Login cho page render + Server 400 retry 1 lan + VM skip POLICY_VIOLATION
- LOGIN: Them 5s wait sau khi detect password page va 2FA page
  → Du da tim thay input, van cho page render day du truoc khi tuong tac
  → Giam loi do JS/DOM chua on dinh khi paste password/OTP
- SERVER: Error 400 (vi pham noi dung) → retry 1 lan roi bao VM POLICY_VIOLATION
  → Truoc: skip ngay (0 retry) → co the mat prompt do ref loi (khong phai prompt)
  → Sau: retry 1 lan (co the fix do ref) → neu van 400 → POLICY_VIOLATION cho VM
- VM: Detect "POLICY_VIOLATION" tu server → skip prompt + tao .SKIP file
  → Truoc: retry 3 lan (sanitize/simplify) → lang phi thoi gian vi server da retry
  → Sau: skip ngay (server da xac nhan la vi pham) → .SKIP file de vm_manager tinh done
- Files: google_login.py, server/chrome_pool.py, modules/smart_engine.py

1.0.650
2026-04-01 (VN)
Fix: Dong bo fingerprint giua login va tao anh - Google thay CUNG 1 fingerprint
- VAN DE: Login Chrome KHONG co fingerprint spoof
  → DrissionFlowAPI inject fingerprint spoof SAU khi connect
  → Google detect fingerprint THAY DOI giua cung session → nghi ngo → 403
- FIX: Inject fingerprint NGAY SAU khi mo Chrome trong login (TRUOC khi navigate)
  → google_login.py: build_fingerprint_js() + Page.addScriptToEvaluateOnNewDocument
  → Save seed vao config/.fingerprint_seed_{worker_id}
  → DrissionFlowAPI doc seed tu file → dung CUNG fingerprint
  → Google thay 1 fingerprint nhat quan tu dau den cuoi
- FLOW:
  → Login: generate seed → save file → CDP inject → navigate Google
  → DrissionFlowAPI: read seed file → inject CUNG seed → tao anh
  → 403 recovery: new seed → save file → login lai dung seed moi
- Files: google_login.py, modules/drission_flow_api.py

1.0.649
2026-04-01 (VN)
Fix: Login - 2FA wait false positive + login success check qua strict
- VAN DE 1: Sau khi nhap password, code check 'challenge' trong URL
  → 'challenge/pwd' (trang password) CHUA CHUYEN MA DA chua 'challenge'
  → Code tuong da qua 2FA → bo qua OTP → login fail
- FIX 1: Track URL truoc khi Enter, doi URL THAY DOI (khong chi check keyword)
  → Chi tiep tuc khi URL thuc su chuyen sang trang moi
- VAN DE 2: Login success check 'accounts.google.com/v3/signin not in url'
  → URL 2FA 'challenge/totp' van chua 'v3/signin' → khong detect progress
  → Timeout 10s qua ngan cho mang cham
- FIX 2: Check specific success URLs (myaccount, labs.google, google.com/search)
  → Exclude ca '/signin' VA '/challenge' paths (ko nham 2FA la fail)
  → Tang timeout 20s → 30s (15 checks × 2s)
- File: google_login.py

1.0.648
2026-04-01 (VN)
Fix: Sync cache → Excel cho SCENES truoc khi tao video (giong refs)
- VAN DE: Scene co media_id trong cache nhung khong co trong Excel
  → Video queue lay media_id tu Excel → khong tim thay → video khong tao duoc
- FIX: Truoc khi queue video, sync cache scene media_ids vao Excel
  → workbook.update_scene(scene_id, media_id=mediaName) + save()
  → 2 cho: resume mode + sau khi tao scenes
  → Excel luon day du du lieu cho video generation
- File: modules/smart_engine.py

1.0.647
2026-04-01 (VN)
Fix: Deadlock refs co anh nhung thieu media_id - sync cache → Excel
- VAN DE: References (loc1-loc12) co file anh + cache co media_id
  → browser_flow_generator skip vi "da co anh" (cache co media_id)
  → smart_engine STOP vi Excel KHONG co media_id → deadlock vinh vien
  → Mau thuan: skip dua tren cache, nhung STOP dua tren Excel
- FIX: Khi cache co media_ids ma Excel khong co → SYNC vao Excel ngay
  → workbook.update_character(id, media_id=mediaName) + save()
  → Excel luon day du du lieu cho cac buoc sau
  → smart_engine STOP check doc Excel → tim thay media_id → khong stop
- File: modules/browser_flow_generator.py
