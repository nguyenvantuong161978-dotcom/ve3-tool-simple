1.0.395
2026-03-24 (VN)
Change: Chrome mode - tao anh truc tiep khong can API interceptor

Thay doi:
- v1.0.395: CHROME MODE - generation_mode: chrome
  * smart_engine.py: Them generate_images_chrome() method
    → Phase 1: Tao reference images (nv/loc) truoc
    → Phase 2: Upload TAT CA refs len gallery 1 lan (CDP setFileInputFiles)
    → Phase 3: Moi scene chon refs tu gallery → type prompt → doi ket qua → download
  * Routing: api | browser | chrome trong run() va _generate_characters_async()
  * drission_flow_api.py: 3 methods moi
    → upload_reference_images_chrome(): Upload refs qua CDP
    → _select_reference_from_gallery(): Click "+" → tim anh → mouse events click
    → generate_image_chrome(): Full flow khong can API interceptor
  * Fix model dropdown: PointerEvent + text search (khong CSS class/Y threshold)
  * Fix _dropdownOpened reset + Video mode verify
  * config/settings.yaml: Them comment cho chrome option
