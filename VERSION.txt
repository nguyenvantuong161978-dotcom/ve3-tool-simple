1.0.404
2026-03-24 (VN)
Change: Fix Chrome 2 infinite disconnect loop - them restart logic

Thay doi:
- v1.0.404: FIX CHROME 2 PARALLEL-VIDEO DISCONNECT LOOP
  * Vấn đề: Chrome 1 restart sau mỗi ảnh → Chrome 2 mất connection → retry vô hạn
  * Fix: Detect "disconnected" error → restart Chrome 2 tự động
  * consecutive_disconnect >= 2 → close Chrome 2 → tạo DrissionFlowAPI mới → setup lại
  * Disconnect error: KHÔNG mark as processed → retry scene sau khi restart
  * Thêm stop_flag check trong for loop (tránh tiếp tục khi đã stop)
