1.0.535
2026-03-29 (VN)
Change: Mode API = LUON dung Chrome local, KHONG gui qua server

- Truoc: Chon mode API nhung co Server URLs → tu dong gui qua server (SAI!)
  * User chon API la muon dung Chrome local (DrissionPage)
  * Nhung code thay local_server_enabled=True → gui server thay Chrome
- Sau: Mode API = luon dung Chrome local
  * Tam tat local_server_enabled khi chay API mode
  * Server URLs chi dung cho mode 'server' va 'api+server'
  * User chon gi duoc nay: API=Chrome, Server=Server, API+Server=ca hai

1.0.534
2026-03-29 (VN)
Change: FIX REUSE project fallback - tao project moi khi textarea khong xuat hien

- BUG: Reuse project URL cu → textarea khong xuat hien → FAIL!
  * Fallback check '/project/' not in URL → URL da co '/project/' → skip tao moi
  * Ket qua: worker CHET, khong phuc hoi duoc
- FIX: Khi reuse fail (textarea khong co):
  * Bo project_url cu (khong dung duoc)
  * Navigate ve Flow home (FLOW_URL)
  * Tao project MOI tu dau
  * Worker phuc hoi binh thuong

1.0.533
2026-03-29 (VN)
Change: STOP scenes khi refs thieu media_id - doi cycle sau

- Truoc: Refs fail (timeout/error) → warn roi chay scenes KHONG CO tham chieu
  * Scenes tao ra KHONG co reference nhan vat/dia diem → chat luong kem
  * Phai tao lai scenes sau khi co refs → lang phi thoi gian + quota
- Sau: Refs chua du media_id → DUNG LAI, khong tao scenes
  * Doi cycle tiep theo khi refs da co media_id
  * Dam bao scenes LUON co day du tham chieu
  * Log ro rang: "[STOP] X/Y refs THIEU media_id!"

1.0.532
2026-03-29 (VN)
Change: Server REUSE project URL + FIX GoogleFlowAPI._log() crash

- SERVER REUSE PROJECT URL (tiet kiem thoi gian):
  * Truoc: MOI LAN restart Chrome (403 → fingerprint moi) → tao project moi (~30-60s)
  * Sau: Restart giu data → navigate thang vao project URL cu (3-5s)
  * Chi tao project moi khi: (1) lan dau setup, (2) sau clear data + login lai
  * Neu project cu khong con (URL loi/het han) → tu dong tao project moi
  * clear_data=True → reset project_url (can tao moi sau login)
- FIX: GoogleFlowAPI._log() crash - them level parameter
  * _log() chi nhan 1 arg nhung co cho goi voi 2 args (message, level)
  * Server 145 tra ve loi: "takes 2 positional arguments but 3 were given"
  * Them level parameter: _log(self, message, level="INFO")

1.0.531
2026-03-29 (VN)
Change: FIX servers bi DOWN oan do refresh_status + tang refresh timeout

- BUG: refresh_status() timeout → tang connect_fail_count → server bi DISABLED
  * Server dang ban (7 VMs gui cung luc) → /api/status cham → refresh fail
  * Moi refresh fail += 1 → 5 lan = server DOWN 60s
  * Nhung server KHONG CHET - chi ban/cham thoi!
- FIX: refresh_status() KHONG tang connect_fail_count
  * Refresh chi la kiem tra, KHONG PHAI submit that
  * Chi mark_submit_failed() (submit that bai) moi disable server
  * Server ban → refresh fail → van available → VM gui duoc
- Tang refresh timeout: 5s → 10s (server ban can nhieu thoi gian hon)

1.0.530
2026-03-29 (VN)
Change: FIX duplicate images - submit timeout KHONG retry + tang timeout 60s

- BUG: Submit timeout bi phan loai sai thanh CONNECT error → retry tren server khac
  * Server co the DA NHAN task (chi response cham) → retry tao DUPLICATE
  * Server lam 2 anh cho 1 prompt, VM chi nhan 1 → LECH so luong
- FIX: Submit timeout = KHONG retry (task co the da nam trong queue)
  * "Proxy request timeout" → mark_task_failed → return False (khong retry)
  * Prompt se duoc retry o vong scan tiep theo (khong mat)
- Tang submit timeout: 30s → 60s (giam chance timeout khi server ban)
- Phan loai loi 3 loai:
  * TIMEOUT: submit cham → KHONG retry (tranh duplicate)
  * CONNECT: server chet → retry server khac (ok vi task chua tao)
  * TASK: Google API loi → retry server khac

1.0.529
2026-03-29 (VN)
Change: Server slot reservation - moi server chi nhan 1 prompt/lan tu VM

- local_pending: Track so prompt VM dang gui cho moi server
  * pick_best_server() tu dong reserve (+1 pending)
  * mark_success/mark_task_failed/mark_submit_failed tu dong release (-1 pending)
  * Sort theo (queue_size + local_pending) → server it viec nhat
- Tranh qua tai: 3 server → 3 thread → moi thread 1 server rieng
  * Thread 1 pick server A (+1) → Thread 2 thay A.pending=1, chon B
  * Xong prompt → release → thread khac co the chon lai
- release_server(): Giam pending khi task xong ma khong can mark success/fail
- Fix: Retry path release server2 dung khi fail

1.0.528
2026-03-29 (VN)
Change: QUEUE-BASED ServerPool - xep hang cho thay vi disable server

- ServerPool redesign: Task da submit vao server = CHAC CHAN se duoc lam
  * Tach connect_fail (server chet) vs task_fail (Google loi)
  * Chi disable server khi KHONG KET NOI DUOC (5 connect fails)
  * Task fail (Google API error) → KHONG disable server (server van OK)
  * Cooldown giam 300s → 60s (server thuong phuc hoi nhanh)
- Smart polling theo queue_position:
  * Truoc: Fixed 90 polls x 2s = 180s timeout → bo cuoc
  * Sau: Dynamic timeout = 180s + position x 90s (cho theo vi tri)
  * Track position changes: "pos 5 → 3 → 1 → processing → done"
  * Stuck detection: position khong giam 180s → canh bao, 360s → timeout
  * Toi thieu 10 phut cho moi task (du cho queue dai)
- Wait-for-server thay vi give up:
  * Truoc: pick_best_server() = None → failed ngay
  * Sau: wait_for_server(300s) → cho + retry moi 10s → tim server
  * Log: "Cho server... (30s/300s, server gan nhat san sang trong ~45s)"
- Phan loai loi chinh xac:
  * Connection error (timeout, refused) → mark_submit_failed → co the disable
  * Task error (Google API, policy) → mark_task_failed → KHONG disable
  * Backward compat: mark_failed() = mark_task_failed()

1.0.527
2026-03-29 (VN)
Change: FIX CRITICAL - Media_id bi mat do Excel ghi de + ref skip + false warning

- BUG 1 (CRITICAL): Media_ids bi MAT giua cac lan chay (10 → 5 media_ids)
  * ROOT CAUSE: _generate_via_local_server() load workbook 1 lan vao memory (line 3679)
    Nhieu threads share CUNG workbook object qua nonlocal
    Moi thread save → ghi TOAN BO in-memory data ra disk → ghi de thay doi cua Chrome 2
  * FIX: Moi lan save → RELOAD Excel tu disk → update 1 field → save lai
    Dung local _wb = PromptWorkbook() thay vi shared workbook
    Bo nonlocal workbook (khong can share nua)
    Dam bao Chrome 1 va Chrome 2 khong ghi de nhau
- BUG 2: Warning "Khong co reference nao tao thanh cong" khi ref da co file
  * Fix: Phan biet 3 TH: (1) co file+media_id, (2) co file ko media_id, (3) ko file
- BUG 3: _generate_via_local_server() skip ref co file nhung KHONG co media_id
  * Fix: Ref (nv/loc) co file nhung khong media_id → van tao lai qua server

1.0.525
2026-03-29 (VN)
Change: Bo checkbox "BAT Local Server Mode" - chi can nhap URL la du

- Bo checkbox thua: Mode Server/API+Server tu force local_server_enabled
- local_server_enabled tu bat khi co URL, tu tat khi xoa URL
- Text box Server URLs luon enabled (khong can bat checkbox moi nhap duoc)
- GUI gon hon, bot 1 buoc thao tac

1.0.524
2026-03-29 (VN)
Change: FIX UnboundLocalError workbook trong _generate_via_local_server

- BUG: "cannot access local variable 'workbook'" khi save Excel trong thread
  * Python closure: workbook reassign o line 3856 → Python coi la local trong _process_one()
  * Line 3838 truy cap truoc khi assign → UnboundLocalError
- FIX: Them "nonlocal workbook" trong _process_one() (cung voi success_count, failed_count)
- Retry 10 lan da handle duoc (save thanh cong o lan 2) nhung fix de tranh warning

1.0.523
2026-03-29 (VN)
Change: FIX 3 bugs trong server mode flow

- BUG 1 (HIGH): Server mode config restore sai - local_server_url khong restore dung
  * Truoc: Chi restore khi old_url rong (logic nguoc) + thieu restore local_server_list
  * Sau: Luon restore tat ca 3 config values (enabled, url, list)
- BUG 2 (LOW): _auto_extract_token() dung .save() thay vi .safe_save()
  * .save() fail khi Excel bi lock → token mat
  * Doi sang .safe_save() (retry khi lock)
- BUG 3 (LOW): Token chi save khi success==1 (miss khi anh dau skip)
  * Doi sang flag _token_saved_to_excel (save 1 lan duy nhat, bat ke thu tu)
  * Them isinstance check cho bearer_token attribute

1.0.522
2026-03-29 (VN)
Change: Luu bearer token vao Excel sau anh dau tien - server mode doc duoc

- API mode (DrissionPage): Sau anh dau tien thanh cong → luu token + project_id vao Excel
  * _generate_images_drission_mode(): stats.success==1 → set_config_value('flow_bearer_token')
  * _auto_extract_token(): Sau khi lay token → luu vao Excel config sheet
- Truoc: Token chi luu trong memory/cache → server mode khong doc duoc
- Sau: Token luu vao Excel → server mode doc tu Excel → khong can mo Chrome lai
- Flow: API mode tao anh → luu token → chuyen Server mode → doc token tu Excel → chay luon

1.0.521
2026-03-29 (VN)
Change: FIX Server mode - khong can checkbox local_server_enabled + skip Chrome login

- BUG: Mode "Server" bao "chua cau hinh Server URLs" du da nhap URLs
  * has_local_server yeu cau local_server_enabled=True (checkbox cu)
  * Chon radio "Server" nhung checkbox tat → has_local_server=False → fail
- FIX: Mode "server" chi can co URL, khong can checkbox
  * Check (local_server_url or server_list) thay vi has_local_server
  * Force local_server_enabled=True + local_server_url trong config
- FIX: Skip Chrome login khi mode=server (khong can Chrome local)
  * Truoc: Login 2 Chrome + warm-up ~6 phut MOI CYCLE (vo ich)
  * Sau: Skip luon, tiet kiem thoi gian

1.0.520
2026-03-29 (VN)
Change: Them nut "Server" vao Generation Mode - gui anh TRUC TIEP qua server

- GUI: Them radio button "Server" cung cho voi API/Browser/Chrome UI/API+Server
- browser_flow_generator.py: Them handler mode 'server' trong generate_from_prompts_auto()
  * Force local_server_enabled=True, goi generate_from_prompts_api()
  * Bao loi neu chua cau hinh Server URLs
- smart_engine.py: Them 'server' vao tat ca mode checks (4 vi tri)
  * Character generation, Scene generation, Step 5 images, proxy_token bypass
- Server mode = chi dung server, KHONG chay Chrome local

1.0.519
2026-03-29 (VN)
Change: FIX API+Server mode - Buoc 1 FORCE chay API (Chrome local) truoc

- BUG: Khi checkbox "BAT Local Server" bat + mode API+Server → gui thang qua server
  * Buoc 1 goi generate_from_prompts_api() nhung ham nay check local_server_enabled
  * Thay local_server_enabled=True → gui qua server ngay → KHONG chay API truoc
- FIX: Tam tat local_server_enabled trong buoc 1 (try/finally dam bao khoi phuc)
  * Buoc 1: FORCE DrissionPage (Chrome local) - khong gui qua server
  * Buoc 2: Neu API fail → bat lai local_server_enabled → chuyen server

1.0.518
2026-03-29 (VN)
Change: Dong bo popup/dialog/quota logic tu API mode sang Server mode

- Server: Them "Da hieu"/"I understand" vao dismiss_popups (giong API mode)
- Server: Them dialog "Toi dong y"/"Agree"/"Accept" bang JS (giong API line 5746-5760)
- Server: Them empty point fallback khi dismiss popup
- Server: Them _is_logged_out() detect logout (giong API mode)
- Server: Them quota/429 error handling - switch model ngay (giong API mode)
- API mode: Them "Da hieu"/"I understand" vao tat ca 4 vi tri dismiss popup

1.0.517
2026-03-29 (VN)
Change: GUI - Day LOCAL SERVER len tren cho de quan sat

- Di chuyen LOCAL PROXY SERVER len ngay sau Generation Mode (truoc API Keys/Chrome)
- Server URLs nhin thay ngay khi mo Settings
- Bo duplicate server block cu o duoi Chrome

1.0.516
2026-03-29 (VN)
Change: NHIEU SERVER - Nhieu VM dung chung nhieu server (load balancing)

- GUI: Doi o nhap 1 URL → Text box nhieu dong (moi dong 1 server URL)
- Luu local_server_list (list) + local_server_url (backward compat)
- Kiem tra ket noi: Test tat ca server, hien popup chi tiet
- ServerPool (da co san): Tu dong chia tai least-queue (server it viec nhat)
- API+Server mode: Ho tro nhieu server khi chuyen tu API sang Server
- Nhieu VM nhap cung danh sach server → tu dong phan bo tai

1.0.515
2026-03-29 (VN)
Change: FIX stats key cho TAT CA return paths - dam bao API+Server khong vo logic

- FIX: Them "stats" key vao 6 early returns thieu (setup fail, import fail, no token, no server)
- DrissionFlowAPI setup failed + chua khoi tao → them stats
- _generate_via_local_server: import fail, no Excel, no token, no server → them stats
- Dam bao MOI return path deu co "stats" key de api+server merge khong bi loi

1.0.514
2026-03-29 (VN)
Change: FIX logic API+Server mode - sua 5 bugs

- FIX: Stats key mismatch - local server return thieu "stats" key → merge stats sai
- FIX: Config mutation khong restore - dung try/finally dam bao khoi phuc
- FIX: proxy_token check chan api+server mode (drission khong can proxy_token)
- FIX: generation_mode chua doc trong generate_images_api() → NameError
- FIX: Tach rieng skipped khoi success trong local server stats
- Dieu kien switch: chi khi API KHONG tao duoc anh MOI nao (success==0 AND failed>0)

1.0.513
2026-03-29 (VN)
Change: Them mode API+Server - chay API truoc, tu dong chuyen Server khi 403 lien tuc

- Them radio button "API + Server" cung cho voi API/Browser/Chrome UI
- Logic: Chay API mode → neu 403 lien tuc khong tao duoc anh → tu dong chuyen sang Server
- Server chay lai nhung anh failed (anh da OK duoc skip)
- Track reset_chrome_profile count de biet khi nao nen chuyen

1.0.512
2026-03-29 (VN)
Change: Mo rong fingerprint pool 53M+ combos + khong trung lap + fix mat giua chung

- Tach modules/fingerprint_data.py: 65 GPU x 15 screen x 11 cores x 5 mem x 1000 canvas = 53M+ combos
- get_unique_seed(): Dam bao moi lan doi fingerprint KHONG trung lap (track 50k seeds)
- Fix: LUON inject fingerprint tu dau (khong doi 403 moi inject)
- Fix: Xoa duplicate hardcode pool o drission_flow_api.py, import tu fingerprint_data.py

1.0.511
2026-03-29 (VN)
Change: Tang Excel save retry tu 3 len 10 lan cho tat ca mode

- Tat ca 5 vi tri save Excel trong browser_flow_generator.py: range(3) → range(10)
- Giam nguy co mat media_id/status khi Excel bi lock boi worker khac

1.0.510
2026-03-29 (VN)
Change: FIX media_id khong luu khi tao anh qua Local Server

- Local server mode chi save status+path, KHONG save media_id → I2V fail
- Them media_id=gen_img.media_name khi update_character() va update_scene()
- Retry 3 lan khi Excel bi lock (reload workbook giua cac retry)
- Pending write fallback neu 3 lan van fail
- Giong het logic save Excel cua API mode (dong 4534-4628)

---
1.0.509
2026-03-29 (VN)
Change: FIX bug _handle_create_with_flow_page not found

- setup() goi _handle_create_with_flow_page() nhung method da bi xoa o v1.0.508
- Doi sang dung _click_create_with_flow() (method moi)
- Chrome-0 bi loi "has no attribute '_handle_create_with_flow_page'" da fix

---
1.0.505
2026-03-29 (VN)
Change: FIX Server GUI bi che nut START

- Tang cua so 650 → 850px
- Giam padding title, separator, buttons
- Giam text box IPv6 va Accounts tu 4 dong → 3 dong
- Dam bao nut UPDATE + START luon hien thi

---
1.0.504
2026-03-29 (VN)
Change: IPv6 cung dung IT NHAT - giong logic account

- get_next_ipv6(): chon IPv6 it dung nhat (khong phai xoay vong)
- Tranh chon IPv6 dang duoc worker khac dung
- Track so lan dung moi IPv6 (_ipv6_usage)
- Ca account va IPv6 deu uu tien cai moi/it dung

---
1.0.503
2026-03-29 (VN)
Change: Account rotation - dung IT NHAT + tranh trung worker

- get_next_account(): chon tai khoan it dung nhat (khong phai xoay vong)
- Tranh chon account dang duoc worker khac dung (phat 1000 diem)
- Track so lan dung moi account (_account_usage)
- Dam bao tai khoan moi luon duoc uu tien

---
1.0.502
2026-03-29 (VN)
Change: Server GUI - Them UPDATE + Tai khoan nhap tu GUI (giong IPv6)

- Them nut UPDATE vao server GUI (giong VM GUI)
- Tai khoan Google nhap vao text box: email|password|2fa_secret
- Luu tai khoan vao config/server_gui.json (load lai khi mo)
- Hien thi tai khoan o setting de biet co bao nhieu
- Account va IPv6 xoay vong lan luot khi 403
- Bo load_all_accounts tu sheet - chi dung accounts tu GUI

---
1.0.501
2026-03-29 (VN)
Change: 403 LOGIC MOI - 3 cap do recovery + retry 10 lan + doi account + bo qua 400

Logic 403 moi (3 cap do):
- Lan 1-3: DOI IPv6 + fingerprint moi (giu data, nhanh)
- Lan 4: CLEAR DATA + DOI IPv6 + login lai (cung account)
- Lan 5+: CLEAR DATA + DOI IPv6 + DOI ACCOUNT MOI tu sheet (reset counter)

Task retry:
- 403: Re-queue task, retry toi da 10 lan (2 vong x 5)
- 400: Bo qua luon, khong retry (content policy / bad request)
- Loi khac: Fail binh thuong

Account rotation:
- Load tat ca tai khoan tu sheet SERVER khi khoi dong
- get_next_account(): Lay account it dung nhat, khong trung worker khac
- Sau khi doi account → reset 403 counter ve 0

---
1.0.500
2026-03-29 (VN)
Change: GUI - Luu settings tu dong (IPv6, Chrome count)

- Luu vao config/server_gui.json khi bam START
- Tu dong load lai khi mo GUI lan sau
- Khong can nhap lai IPv6 moi lan

---
1.0.499
2026-03-29 (VN)
Change: IPv6 CHI tu GUI - Bo lay tu Google Sheet

- Bo logic lay IPv6 tu sheet SERVER col C
- IPv6 chi tu text box trong GUI setting
- Moi worker duoc gan IPv6 tu GUI list (quay vong neu nhieu worker)

---
1.0.498
2026-03-29 (VN)
Change: GUI - Them nut THEM VAO MAY + TEST IPv6

- THEM VAO MAY: Chay netsh de add IPv6 vao network interface
- TEST IPv6: Kiem tra IPv6 nao da assign + bind duoc
- Hien popup ket qua chi tiet (OK/CHUA ADD/BIND FAIL)

---
1.0.497
2026-03-29 (VN)
Change: 403 → DOI IPv6 NGAY moi lan (khong doi 9 lan)

Truoc: 403 x5 (model 0) → switch model → 403 x2 (model 1) → ... → 9 lan moi doi IPv6
Sau: MOI LAN 403 → doi IPv6 + fingerprint moi + restart Chrome

---
1.0.496
2026-03-29 (VN)
Change: FIX WORKERS KHONG CHAY - Retry setup + khong bo worker khi fail

Fix:
- app.py: Setup fail → RETRY 3 lan (truoc: fail 1 lan → bo luon worker)
- app.py: Doi 10s giua cac retry (cho Chrome oi dinh)
- chrome_pool.py: setup_all() chay SONG SONG (truoc: tuan tu, 1 fail → block het)
- chrome_pool.py: start_workers() khoi dong TAT CA workers (truoc: chi ready moi start)
- chrome_pool.py: Worker loop tu retry setup neu chua ready

---
1.0.495
2026-03-29 (VN)
Change: Bo nut TACH/GOP khoi GUI server - fix START bi che

---
1.0.494
2026-03-28 (VN)
Change: FIX 403 LOGIC + FINGERPRINT SPOOF TIMING + GOP BUOC XOA DATA

Fix 403 Logic (CRITICAL):
- restart_with_new_fingerprint(clear_data=False): KHONG xoa Chrome data mac dinh
  * Truoc: MOI lan 403 → xoa data → login lai (~2 phut downtime) = SAI!
  * Sau: Chi restart Chrome + fingerprint moi (nhanh, giu login)
  * clear_data=True CHI KHI da het 3 models
- Gop buoc: Het 3 models → DOI IPv6 + XOA DATA + LOGIN LAI (1 buoc thay vi 2)
  * Truoc: 2 buoc rieng: (1) xoa data + login, (2) doi IPv6 + xoa data + login = THUA
  * Sau: 1 buoc: doi IPv6 truoc → xoa data → login lai
  * Ap dung cho: chrome_pool.py, worker.py, drission_flow_api.py (ca image + video)

Fix Fingerprint Spoof:
- Truoc: run_js() inject SAU page load → Google thay fingerprint THAT truoc
- Sau: CDP Page.addScriptToEvaluateOnNewDocument inject TRUOC page scripts
  * Inject NGAY SAU khi mo Chrome, TRUOC khi navigate toi Google
  * Google chi thay fingerprint GIA tu dau den cuoi
  * Ap dung cho CA server mode (chrome_session.py) VA api mode (drission_flow_api.py)

Flow 403 moi:
  403 x1-4: cleanup browser data → restart Chrome (GIU DATA) + fingerprint moi
  403 x5:   switch model (model 0→1→2)
  Het 3 models: DOI IPv6 + XOA DATA + LOGIN LAI (1 buoc)

---
1.0.492
2026-03-28 (VN)
Change: FIX Bind warning spam + /api/queue crash

Fix:
- IPv6 proxy: Bind warning chi log 1 lan (tranh spam hang tram dong)
- IPv6 proxy: "No IPv6 address for X" chi log 1 lan cho moi host
- /api/queue: Fix TypeError khi w.current_task_id la None (race condition)

---
1.0.491
2026-03-28 (VN)
Change: SERVER IPv6 ROTATION tu sheet + GUI nhap bo sung

Thay doi:
- ChromePool luu TAT CA IPv6 tu sheet SERVER col C vao _ipv6_list
- Them get_next_ipv6(current): lay IPv6 tiep theo tu list (quay vong, bo qua IP hien tai)
- Khi 403 sau clear data → goi get_next_ipv6() → truyen cho session.rotate_ipv6(new_ip)
- ChromeSession.rotate_ipv6(new_ip): nhan IPv6 truc tiep thay vi tu tim
  + Restart SOCKS5 proxy voi IP moi
  + Tao proxy moi ke ca khi truoc do chua co
- GUI: Them o nhap "IPv6 bo sung" (Text box, moi dong 1 IP)
  + IPv6 tu GUI duoc them vao _ipv6_list cung voi IPv6 tu sheet
  + De trong neu chi dung IPv6 tu sheet

Fix:
- Them self._cleared_data_for_403 = False trong __init__ (tranh skip luot 1)
- Them self._current_model_index = 0 trong __init__ (tranh dung model sai)

---
1.0.490
2026-03-28 (VN)
Change: FIX khoi tao bien 403 trong __init__ + 403 TACH RIENG + IPv6 ROTATION

---
1.0.489
2026-03-28 (VN)
Change: 403 HANDLING TACH RIENG + IPv6 ROTATION SAU MOI LUOT 403

Thay doi:
- Tach 403 thanh 3 methods rieng, de maintain:
  + _handle_image_403(tag): ANH - co switch model
  + _handle_video_403(tag): VIDEO - KHONG switch model
  + _rotate_ipv6(tag): Helper doi IPv6

- FLOW 403 CHO ANH (_handle_image_403):
  LUOT 1: Model 0 (5x) → Model 1 (2x) → Model 2 (2x) → clear data + login lai
  LUOT 2: Van 403 sau clear data → DOI IPv6 → restart lai tu model 0
  Moi lan restart → fingerprint moi + cleanup browser data

- FLOW 403 CHO VIDEO (_handle_video_403):
  LUOT 1: 403 x1-4 restart + fingerprint → 403 x5 clear data + login lai
  LUOT 2: Van 403 sau clear data → DOI IPv6 → restart lai tu dau
  KHONG switch model

- IPv6 ROTATION (_rotate_ipv6):
  + Activate IPv6 neu chua bat (goi _activate_ipv6)
  + Chi worker_id=0 (Chrome 1) rotate, worker khac dung IP hien tai
  + Dung ipv6_rotator de doi sang IP moi tu config/ipv6_list.txt

- SERVER MODE (chrome_pool.py + chrome_session.py):
  + Them ChromeSession.rotate_ipv6(): doi IPv6 + restart SOCKS5 proxy
  + chrome_pool.py: Khi da clear data van 403 → goi rotate_ipv6() truoc restart

- AP DUNG CHO CA API MODE VA SERVER MODE

---
1.0.487
2026-03-28 (VN)
Change: SERVER 403 LOGIC GIONG API MODE - switch model + fingerprint

Thay doi:
- v1.0.487: SERVER 403 GIONG API MODE
  * Server 403 logic giong het API mode (drission_flow_api.py)
  * Model 0 (Nano Banana Pro): 5 lan 403 → switch model 1
  * Model 1 (Nano Banana 2): 2 lan 403 → switch model 2
  * Model 2 (Imagen 4): 2 lan 403 → clear data + reset model 0 + login lai
  * Moi lan restart → fingerprint moi (seed random)
  * Cleanup browser data ngay khi gap 403
  * generate_image() dung _current_model_index khi da switch
- v1.0.486: FINGERPRINT SPOOF KHI 403
  * Moi lan bi 403 → thay doi WebGL renderer, Canvas noise, CPU cores, RAM, Screen
  * Google nhan dien may qua fingerprint → doi IP khong du → can doi fingerprint
  * SERVER: chrome_session.py + chrome_pool.py - 403 x3 → restart voi fingerprint moi
  * TOOL (API mode): drission_flow_api.py - moi lan 403 restart → fingerprint moi
  * Danh sach GPU gia: RTX 3060/4060, GTX 1660, RX 580/5700XT, UHD 770, Iris Xe
  * Moi lan 403 seed khac nhau → fingerprint khac nhau hoan toan
  * Test: test_fingerprint_spoof.py (chay doc lap de verify)
- v1.0.485: FIX 3 BUGS TRONG LOCAL SERVER MODE
  * BUG 1 (CRITICAL): Chrome 1 va Chrome 2 cung tao GIONG NHAU (khong chia chan/le)
    - Root cause: Logic filter chan/le nam SAU redirect local server → khong bao gio chay
    - Fix: Di chuyen parallel filter LEN TRUOC local server redirect
    - Chrome 0 (Chrome 1): scenes chan (2,4,6...) + references (nv/loc)
    - Chrome 1 (Chrome 2): scenes le (1,3,5...)
  * BUG 2: Thumbnail loi "cannot access local variable 'excel_media_ids'"
    - Root cause: excel_media_ids chua duoc load truoc khi goi _generate_thumbnails_via_server
    - Fix: Them wb.get_media_ids() truoc khi goi server
  * BUG 3: SKIP_VALIDATOR hien ERROR voi traceback (khong dep)
    - Fix: Check "SKIP_VALIDATOR" trong except truoc khi log error
- v1.0.484: TANG TIMEOUT - VM polling 60 → 90 attempts (120s → 180s)
  * Van de: Server cho Google 120s + queue wait → VM polling 120s co the het truoc
  * Fix: Tang max_attempts tu 60 → 90 (= 180s) du buffer cho queue + network
- v1.0.483: FIX CRITICAL - References bi mat trong local server mode
  * BUG: _generate_via_local_server KHONG truyen image_inputs khi goi api.generate_images()
    - pending_prompts khong luu reference_files tu prompt data
    - api.generate_images() goi KHONG co image_inputs parameter
    → Tat ca anh tao qua server KHONG CO tham chieu (reference images)
  * FIX:
    - Load media_ids tu Excel (nv1 → media_id, loc1 → media_id)
    - Luu reference_files vao pending_prompts
    - Build image_inputs tu reference_files + media_ids
    - Truyen image_inputs vao api.generate_images() (ca lan dau + retry)
    - Log so references: "[7/441] 105 → server (q=0) +2ref"
- v1.0.482: CHROME 2 LOCAL SERVER SUPPORT
  * Thumbnail: Neu local_server_enabled → dung GoogleFlowAPI qua server (khong mo Chrome)
    - Landscape (3 anh) + Portrait (3 anh) deu qua server
    - Reference images (imageInputs) van duoc truyen day du
  * Validator: Neu local_server_enabled → skip Chrome validator
    - Mark refs as verified (server se validate khi tao anh)
    - Chrome 2 khong can mo Chrome nao khi co local server
- v1.0.481: SERVER - Them lai JS_CLEANUP sau moi generate_image()
  * Sau moi request (thanh cong/loi): xoa localStorage, sessionStorage, IndexedDB, cookies, cache, service workers
  * Tranh Google track/flag browser qua stored data
  * Giong flow v1.0.198 (da bo khi lam giong test file, nay them lai)
- v1.0.480: FIX 3 BUGS TRONG _generate_via_local_server
  * BUG 1: Anh khong luu - images[0] la GeneratedImage object, KHONG phai bytes/str
    - Code cu: isinstance(img_data, bytes) → KHONG BAO GIO match → anh mat
    - Fix: Dung gen_img.base64_data hoac gen_img.url de luu anh
  * BUG 2: Ten file sai - scene_095.png → 95.png (giong smart_engine.py)
    - Code cu: f"scene_{int(prompt_id):03d}.png"
    - Fix: f"{prompt_id}.png" (giong cac cho khac trong codebase)
  * BUG 3: Excel error - 'PromptWorkbook' has no attribute 'update_scene_status'
    - Code cu: workbook.update_scene_status() / update_character_status()
    - Fix: workbook.update_scene() / update_character() (methods thuc su co)
- v1.0.479: Fix interceptor reset
- v1.0.478: REFERENCE IMAGES (imageInputs)
  * server/chrome_session.py: build_interceptor_js() nhan image_inputs
    - Inject imageInputs vao body.requests[] khi gui Google
    - generate_image() nhan image_inputs parameter
  * server/chrome_pool.py: Truyen image_inputs tu task xuong generate_image()
  * server/app.py: Lay imageInputs tu request VM, luu vao task
  * Test OK: 3/3 anh (1 goc + 2 co reference) thanh cong
  * imageInputType = "IMAGE_INPUT_TYPE_REFERENCE" (giong tool)
- v1.0.477: FIX 403 - GIONG TEST FILE
  * generate_image() copy y het flow tu test_local_proxy.py (da hoat dong)
  * Bo JS_CLEANUP truoc request (pha reCAPTCHA state → 403)
  * Giam recaptcha wait 8s → 4s (giong test)
  * Giu interceptor thay token + projectId (giong test - da chay OK)
- v1.0.476: SERVER GUI (Tkinter)
  * server/server_gui.py: Giao dien cai dat + monitor
    - Trang 1: Cai dat IPv6 ON/OFF, so Chrome (1-5/tat ca)
    - Bam START → khoi dong Flask + Chrome workers
    - Trang 2: Hien trang thai workers, stats, logs realtime
  * START_SERVER.bat → chay server_gui.py
- v1.0.475: DASHBOARD SETTINGS (web)
  * Mo dashboard → thay panel cai dat truoc khi start
  * Toggle IPv6 ON/OFF (default: ON)
  * Chon so Chrome: 1/2/3/4/5/tat ca
  * Bam START → setup Chrome theo cai dat
  * CLI args: --no-ipv6, --chrome N, --auto (tu dong start)
- v1.0.474: INSTANT WORKER START
  * Chrome nao setup xong → start worker loop NGAY
  * Khong doi tat ca worker setup xong moi bat dau
  * Worker cham khong block worker nhanh
- v1.0.473: IPv6 SUPPORT - Moi Chrome dung IPv6 rieng
  * server/chrome_session.py: Nhan ipv6 param, tao SOCKS5 proxy tu dong
    - Moi Chrome co 1 SOCKS5 proxy rieng (port = debug_port + 200)
    - Chrome ket noi qua --proxy-server=socks5://127.0.0.1:{proxy_port}
    - Traffic di qua IPv6 address duoc assign tu Sheet SERVER col C
  * server/app.py: Truyen worker.ipv6 vao ChromeSession
  * server/chrome_pool.py: Truyen worker.ipv6 vao ChromeSession
  * server/worker.py: Truyen ipv6 vao ChromeSession
  * Fix 403 reCAPTCHA: Root cause la tat ca Chrome dung chung IPv4
- v1.0.472: WEB DASHBOARD + SINGLE PROCESS
  * server/app.py v4.0: Single process + Web Dashboard
    - Web dashboard tai http://server:5000/ (dark theme, auto-refresh 2s)
    - Hien thi: workers status, queue, processing, logs
    - Chrome setup SONG SONG bang threads (khong can CMD rieng)
    - Flask khoi dong NGAY, Chrome setup phia sau
    - 1 CMD duy nhat thay vi 6 CMD
  * server/worker.py: Fix polling qua nhanh (1s -> 3s)
  * server/worker.py: Fix error dict -> string conversion
- v1.0.471: MULTI-PROCESS SERVER + FIXES
  * FIX chrome_pool.py: result['error'] la dict -> TypeError crash
  * FIX browser_flow_generator.py: Doc flow_project_id tu Excel
  * server/worker.py + start_server.py: Multi-process mode (backup)
- v1.0.470: SMART LOGIN - chi login khi account sai
- v1.0.467: MULTI-CHROME SERVER (ChromePool)
