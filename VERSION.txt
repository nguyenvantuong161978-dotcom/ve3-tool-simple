1.0.649
2026-04-01 (VN)
Fix: Login - 2FA wait false positive + login success check qua strict
- VAN DE 1: Sau khi nhap password, code check 'challenge' trong URL
  → 'challenge/pwd' (trang password) CHUA CHUYEN MA DA chua 'challenge'
  → Code tuong da qua 2FA → bo qua OTP → login fail
- FIX 1: Track URL truoc khi Enter, doi URL THAY DOI (khong chi check keyword)
  → Chi tiep tuc khi URL thuc su chuyen sang trang moi
- VAN DE 2: Login success check 'accounts.google.com/v3/signin not in url'
  → URL 2FA 'challenge/totp' van chua 'v3/signin' → khong detect progress
  → Timeout 10s qua ngan cho mang cham
- FIX 2: Check specific success URLs (myaccount, labs.google, google.com/search)
  → Exclude ca '/signin' VA '/challenge' paths (ko nham 2FA la fail)
  → Tang timeout 20s → 30s (15 checks × 2s)
- File: google_login.py

1.0.648
2026-04-01 (VN)
Fix: Sync cache → Excel cho SCENES truoc khi tao video (giong refs)
- VAN DE: Scene co media_id trong cache nhung khong co trong Excel
  → Video queue lay media_id tu Excel → khong tim thay → video khong tao duoc
- FIX: Truoc khi queue video, sync cache scene media_ids vao Excel
  → workbook.update_scene(scene_id, media_id=mediaName) + save()
  → 2 cho: resume mode + sau khi tao scenes
  → Excel luon day du du lieu cho video generation
- File: modules/smart_engine.py

1.0.647
2026-04-01 (VN)
Fix: Deadlock refs co anh nhung thieu media_id - sync cache → Excel
- VAN DE: References (loc1-loc12) co file anh + cache co media_id
  → browser_flow_generator skip vi "da co anh" (cache co media_id)
  → smart_engine STOP vi Excel KHONG co media_id → deadlock vinh vien
  → Mau thuan: skip dua tren cache, nhung STOP dua tren Excel
- FIX: Khi cache co media_ids ma Excel khong co → SYNC vao Excel ngay
  → workbook.update_character(id, media_id=mediaName) + save()
  → Excel luon day du du lieu cho cac buoc sau
  → smart_engine STOP check doc Excel → tim thay media_id → khong stop
- File: modules/browser_flow_generator.py

1.0.646
2026-04-01 (VN)
Fix: Login KHONG dong Chrome - DrissionFlowAPI dung luon session dang mo
- VAN DE: Login thanh cong → warm-up → driver.quit() dong Chrome
  → DrissionFlowAPI mo Chrome lai tren cung port → co luc mat session (intermittent)
  → Chrome chua flush cookies/session xuong disk truoc khi quit
- FIX: Login xong KHONG dong Chrome, de DrissionFlowAPI connect vao luon
  → Chrome van chay tren port 9222+worker_id voi session da login
  → DrissionFlowAPI.setup() → ChromiumPage(options) → detect port da co → connect vao
  → KHONG can mo Chrome moi → KHONG mat session
- FLOW MOI:
  → Pre-login: Chrome 1 (port 9222) login → giu mo | Chrome 2 (port 9223) login → giu mo
  → DrissionFlowAPI worker 0 → connect Chrome 1 | worker 1 → connect Chrome 2
  → 403 recovery: reset_chrome_profile() → self.driver=None → _auto_login_google() → Chrome mo moi
    → _warmup_after_login() → setup() → ChromiumPage connect Chrome login vua mo
- File: google_login.py

1.0.645
2026-04-01 (VN)
Fix: Password detection dung JS + giam NDP log spam
- VAN DE 1: Password input LUON "NOT FOUND after 20s" o moi lan login
  → 6 DrissionPage selectors × 0.5s timeout × 20 vong = 72s lang phi
  → Google da doi DOM, khong selector nao match
  → Login chi thanh cong khi blind paste tinh co dung cho
- FIX 1: Dung JavaScript detect password input (nhanh + reliable)
  → JS chay truc tiep trong browser, tim input bang 6 cach (type, name, autocomplete, aria-label, #password div, querySelectorAll)
  → Tim thay → auto focus + click → san sang cho Ctrl+V
  → Loop 15s thuc te (thay vi 72s)
  → Fallback: DrissionPage selectors neu JS khong tim thay
  → Log URL hien tai khi fail de debug
- VAN DE 2: NDP keepalive log spam "no reply (fail #1)" roi recovery ngay
  → Fail #1 la binh thuong (1 packet drop), khong can log
- FIX 2: Chi log tu fail #2 tro len, recovery chi log neu fail >= 2
- Files: google_login.py, modules/proxy_providers/ipv6_provider.py

1.0.644
2026-04-01 (VN)
Fix: NDP keepalive ping DEFAULT gateway thay vi gateway rieng cua worker
- VAN DE (v1.0.643): Worker o subnet khac (VD 3072::) ping gateway rieng (3072::1)
  → Gateway rieng KHONG reachable vi on-link route fail → NDP keepalive lien tuc fail
  → Tuy Chrome van chay (SOCKS5 bind OK) nhung log spam "no reply"
- FIX: Keepalive va setup ping deu dung DEFAULT gateway (gateway cua route ::/0)
  → Default gateway LUON reachable (co route ::/0 tro den no)
  → NDP cache cho default gateway duoc giu alive
  → Reply packets: MikroTik NDP tim MAC cua destination IP → Windows respond (tu dong)
- Files: modules/proxy_providers/ipv6_provider.py

v1.0.643
2026-04-01 (VN)
Fix: Single-route architecture + NDP keepalive - on dinh cho 10+ Chrome instances
- VAN DE: Nhieu route ::/0 (v1.0.640) → Windows chon 1 → worker khac mat mang bat thuong
  → NDP cache expire sau 30-60s → MikroTik khong tim duoc MAC → mat ket noi
- FIX 1: CHI 1 route ::/0 duy nhat (worker dau tien them, worker sau KHONG them)
  → _default_route_gateway + _route_lock tranh race condition
  → Worker chi add IP address + on-link route /64
  → SOCKS5 proxy bind() source IP rieng → Google thay IP khac nhau
  → MikroTik forward tu bat ky source subnet (cung bridge, da xac nhan)
- FIX 2: NDP keepalive thread (NDPKeepalive class)
  → Moi worker ping gateway tu source IP cua minh moi 20s
  → Giu NDP cache alive → khong mat mang sau 30-60s
  → Log canh bao khi gateway khong reply
- FIX 3: ipv6_rotator.py tuong thich single-route
  → Khi IPv6Provider._default_route_gateway da co → KHONG xoa/them ::/0
  → Chi doi on-link route khi rotate
- FIX 4: Stop cleanup - chi xoa ::/0 khi worker cuoi cung stop
- Files: modules/proxy_providers/ipv6_provider.py, modules/ipv6_rotator.py

v1.0.642
2026-03-31 (VN)
Revert: Quay lai v1.0.640 - moi worker them route ::/0 rieng
- v1.0.641 (chi 1 route ::/0) lam Chrome-1 LUON mat mang (NDP cross-subnet fail)
- v1.0.640: Moi worker them ::/0 rieng, _cleanup_old_routes() bao ve route cua nhau
- Co luc bi luc khong (tuy Windows metric), nhung an toan hon 641
- Se tiep tuc dieu tra root cause sau

v1.0.641
2026-03-31 (VN)
Fix: Server 2 Chrome mat mang - chi dung 1 route ::/0 duy nhat
- VAN DE: 2 route ::/0 tren cung interface → Windows chon 1 → worker con lai mat mang
- v1.0.640 khong du: giu 2 route van xung dot (co luc bi luc khong tuy metric)
- FIX: Chi worker DAU TIEN them ::/0, worker sau KHONG them (dung chung gateway)
  → IPv6Provider._default_route_owner track ai so huu ::/0
  → Worker rotate: chi cap nhat ::/0 neu la owner
  → Worker stop: reset owner → worker tiep theo tu them
- DA XAC NHAN: MikroTik forward traffic tu bat ky subnet (cung bridge, khong filter src)
- Google van thay 2 IP khac nhau (SOCKS proxy bind source IP rieng)

v1.0.640
2026-03-31 (VN)
Fix: Server 2 Chrome - Chrome-0 mat mang khi Chrome-1 setup/rotate IPv6
- VAN DE: _cleanup_old_routes() XOA default route ::/0 cua worker KHAC
  → Chrome-1 setup → xoa route cua Chrome-0 → Chrome-0 mat mang vinh vien
- FIX: Track gateway cua tat ca worker dang active (IPv6Provider._active_gateways)
  → _cleanup_old_routes() CHI xoa route khong thuoc worker nao
  → Moi worker giu route rieng, khong anh huong nhau
- Files: modules/proxy_providers/ipv6_provider.py, modules/ipv6_rotator.py

v1.0.639
2026-03-31 (VN)
GUI: Settings 1 trang cuon duy nhat - fix do (khong auto-load account info)
- Bo Notebook Tabs, quay lai 1 trang cuon gon gang
- Khong tu dong load tai khoan khi mo Settings (nguyen nhan do)
- Tai nguyen hien thi inline: Chrome: OK | API Key: OK | ...

v1.0.638
2026-03-31 (VN)
GUI: Settings dung Notebook Tabs - gon gang, de dung
- 4 tabs: NOI DUNG | KET NOI | PROXY | TAI KHOAN
- NOI DUNG: Topic, Mode, Generation Mode, Kiem tra tai nguyen
- KET NOI: Server URLs, SMB Share (ket noi o mang)
- PROXY: Loai proxy (IPv6 File/Pool/Webshare), cau hinh + test
- TAI KHOAN: API Keys, Chrome Portable, Veo3 Accounts
- Moi tab co scroll rieng, khong can cuon dai nhu truoc
- Bo nut SETUP VM - da gop vao Settings

v1.0.637
2026-03-31 (VN)
GUI: Gop Setup VM vao Settings - tat ca cau hinh trong 1 cua so duy nhat
- Chuyen SMB Share (ket noi o mang) vao Settings
- Chuyen Proxy/IP Rotation (IPv6 File, IPv6 Pool, Webshare) vao Settings
- Bo nut SETUP VM tren thanh cong cu (giao dien gon hon)
- Tat ca cau hinh: Topic, Mode, Generation, Server, API Keys, Chrome, Accounts, SMB, Proxy

v1.0.636
2026-03-31 (VN)
Fix: Server video 400 - doi URL T2V→I2V + doi model t2v→r2v (giong API mode) + timeout 300s
- VAN DE: Server interceptor inject referenceImages nhung KHONG doi URL endpoint
  → Chrome gui den batchAsyncGenerateVideoText (T2V) khong chap nhan referenceImages → 400
  → API mode hoat dong vi DOI URL sang batchAsyncGenerateVideoReferenceImages (I2V)
- FIX 1: Doi URL batchAsyncGenerateVideoText → batchAsyncGenerateVideoReferenceImages
- FIX 2: Doi model _t2v_ → _r2v_ + them _landscape truoc _ultra (I2V format)
- FIX 3: Video timeout 60s → 300s (video can nhieu thoi gian hon anh)

v1.0.635
2026-03-31 (VN)
Fix: Server Chrome - detect IPv6 chet qua proxy connect failures, rotate NGAY thay vi doi 403
- VAN DE: IPv6 chet sau setup nhung worker chi rotate khi gap 403
  → 429/400 do IPv6 chet bi xu ly sai: switch model hoac skip task
  → Chrome chay 5-10 phut voi IPv6 chet, skip nhieu task truoc khi rotate
- FIX 1: ipv6_proxy.py track _connect_failures (tang khi fail, reset khi success)
- FIX 2: chrome_pool.py check proxy connect failures khi gap 429/400
  → Neu >= 5 connect failures → IPv6 chet → rotate IPv6 + restart (nhu 403)
  → Task duoc re-queue thay vi skip/fail
- Ket qua: Rotate trong <30s thay vi 5-10 phut, giam mat task

v1.0.634
2026-03-31 (VN)
Fix: Server video - khong bat buoc Lower Priority model, interceptor thay model tu VM
- Server Chrome chi can o Video mode (chon model bat ky co san)
- Interceptor thay videoModelKey tu payload VM gui (VD: lower priority)
- Tai khoan server khong can co Lower Priority, chi can VM co quyen la du
- Vi interceptor thay bearer token → Google check quyen cua VM, khong phai server

v1.0.633
2026-03-31 (VN)
Fix: Server worker - IPv6 mat mang sau setup → rotate TRUOC khi retry
- VAN DE: Chrome-1 IPv6 pass test luc setup nhung mat ket noi ngay sau
  → Chrome load Flow page 20 lan fail, worker khong bao gio ready
  → Retry setup nhung IPv6 van chet → worker dung hoan toan
- FIX 1: Rotate IPv6 TRUOC khi retry setup (truoc: rotate SAU khi fail)
- FIX 2: Kill Chrome cu truoc khi retry (tranh chiem port + dung IPv6 cu)
- FIX 3: Close session cu trong _setup_single_worker khi retry

v1.0.632
2026-03-31 (VN)
Fix: Server Chrome video - THUC SU chuyen sang Video mode (khong con FORCE MODE)
- Truoc: Server Chrome o Image mode, interceptor doi URL sang video → Google reject (recaptcha sai mode)
- Sau: Click chuyen sang Video mode thuc su (Video tab → Thanh phan → 16:9 → x1 → Lower Priority)
- Interceptor chi thay token + projectId + inject mediaId (KHONG doi URL)
- Chrome gui VIDEO request voi recaptchaToken hop le cho video mode
- Giong cach VM Chrome lam (JS_SWITCH_TO_T2V_MODE trong drission_flow_api.py)

v1.0.631
2026-03-31 (VN)
Feature: Video generation (I2V) ho tro SERVER MODE
- Truoc: _video_worker_loop LUON dung Chrome local (drission_api) → server mode khong tao video
- Fix: Check generation_mode, neu 'server'/'api+server' → dung GoogleFlowAPI gui qua server
- Server Chrome dung FORCE MODE (JS interceptor) tao video
- Flow: VM gui media_id → server /api/fix/create-video-veo3 → operations → VM poll Google lay video

v1.0.630
2026-03-31 (VN)
Fix: IPv6 Pool GUI bi dong/treo khi rotate_ip dang chay
- Root cause: _refresh_ui() chay tren main thread, goi pool.get_status() can _lock
- Khi rotate_ip() giu _lock (MikroTik API 2-5s+) → main thread block → GUI dong
- Fix: Fetch pool data trong background thread, chi update widget tren main thread

v1.0.629
2026-03-31 (VN)
Feature: Server Chrome tao video tu anh (I2V - Image to Video)
- Endpoint moi: POST /api/fix/create-video-veo3
- Server Chrome dung FORCE MODE: o image mode, interceptor convert sang video request
- VM gui media_id (tu anh da tao) → server tra operations → VM poll Google lay video
- Timeout cho anh tang 120s → 180s

v1.0.628
Fix: Server worker loop - auto rotate IPv6 khi setup fail (IPv6 mat mang sau test ban dau)
- Khi worker setup fail → rotate IPv6 qua pool client → re-setup proxy → retry
- Xu ly truong hop IPv6 pass test luc dau nhung mat ket noi sau do

v1.0.627
Fix: Server IPv6 - test internet thuc te (curl Google) thay vi chi ping gateway
- Neu IPv6 khong co internet → auto rotate sang IPv6 moi tu pool
- Truoc: chi ping gateway (local) → "OK" nhung Chrome khong ket noi duoc

v1.0.626
Fix: Token refresh 401 - dung DrissionPage thay PyAutoGUI (F12+JS khong hoat dong)

v1.0.625
Fix: IPv6 API server bi dong/treo - doi HTTPServer thanh ThreadingHTTPServer

v1.0.624
Add: ve3/ folder vao danh sach update (ZIP/manual) de may khac cap nhat duoc

v1.0.623
Fix: Login - doi URL chuyen sang password page + nhieu selector

- VAN DE: input[type="password"] khong tim thay sau 15s
  * Sau khi nhap email, code chi sleep(3) roi tim password input
  * Mang cham → trang chua chuyen sang /challenge/pwd → khong co input
  * Chi tim 1 selector: input[type="password"] → Google doi thi fail
- FIX 1: Doi URL chuyen sang /challenge/pwd truoc (max 20s)
  * Thay sleep(3) bang loop check URL
  * Detect "challenge/pwd" hoac "challenge/password"
- FIX 2: Thu nhieu selector thay vi chi 1
  * input[type="password"], input[name="Passwd"], input[name="password"]
  * input[autocomplete="current-password"], #password input, div[id="password"] input
  * Max 20s (tang tu 15s)
- File: google_login.py

v1.0.622
2026-03-31 (VN)
Fix: Pool lay qua nhieu IP - register sai format + khong release IP cu

- BUG 1: register_ip so sanh "3065" vs "65" → LUON fail → luon get_ip moi
  * Pool luu subnet_hex = "65" (2 ky tu)
  * register extract = "3065" (4 ky tu) → khong match
  * FIX: Luon lay 2 ky tu cuoi: parts[3][-2:]
- BUG 2: VM khong release IP cu truoc khi lay IP moi
  * Register fail → get_ip → IP cu van "in_use" mai
  * 2 VM × nhieu lan restart = chiem 10+ IP
  * FIX: Release IP cu truoc get_ip
- BUG 3: release_ip chi match exact address
  * Sau RESET, pool co IP moi cho cung subnet → exact match fail
  * FIX: Fallback match theo subnet_hex
- Files: ipv6/ipv6_pool.py, modules/ipv6_rotator.py

v1.0.621
2026-03-31 (VN)
Fix: Login fail khi mang cham - doi element thay vi sleep co dinh

- VAN DE: Code dung time.sleep(3) co dinh → mang cham thi trang chua load xong
  * Password input chua xuat hien → Ctrl+V paste vao void
  * OTP input chua xuat hien → OTP khong duoc nhap
  * Chi check 5 lan (10s) → timeout qua ngan
- FIX: Doi element xuat hien thay vi sleep:
  * Password: doi input[type="password"] xuat hien (max 15s)
  * 2FA page: doi URL chuyen sang "challenge" (max 10s)
  * OTP: doi input[type="tel"] hoac #totpPin xuat hien (max 15s)
  * Click vao input truoc khi paste (dam bao focus)
  * Tang login check 5→10 lan (20s thay vi 10s)
- File: google_login.py

v1.0.620
2026-03-31 (VN)
Fix: Firewall bi treo 80s - bo duplicate + check exists truoc khi tao

- VAN DE: Firewall chay 2 LAN (ipv6_provider + drission_flow_api)
  * Delete 20 rules × 5s timeout = 100s cho
  * Tao lai 8 rules = them 20s
  * Tong: 80-120s bi "dong" moi lan mo Chrome
- FIX 1: Bo firewall trong ipv6_provider.py (da chuyen sang drission_flow_api.py)
- FIX 2: Check rules da ton tai → skip (khong delete/recreate)
- FIX 3: Giam range 20→8, timeout 5s→3s
- Ket qua: Tu 80s → <1s (neu rules da co)
- Files: modules/drission_flow_api.py, modules/proxy_providers/ipv6_provider.py

v1.0.619
2026-03-31 (VN)
Fix: Block IPv4 TRUOC khi mo Chrome (google_login.py)

- VAN DE: Firewall code chi chay trong DrissionFlowAPI.setup_and_open_chrome()
  * google_login.py mo Chrome RIENG (khong qua DrissionFlowAPI)
  * Chrome login mo TRUOC → da ket noi IPv4 → firewall sau vo nghia
- FIX: Goi _block_ipv4_for_chrome_static() trong google_login.py
  * Block IPv4 TRUOC KHI mo Chrome
  * Chrome mo ra da khong the dung IPv4
  * Static method: goi duoc tu bat ky dau (khong can instance)
- Files: google_login.py, modules/drission_flow_api.py

v1.0.618
2026-03-31 (VN)
Fix: Di chuyen firewall block IPv4 vao drission_flow_api.py (file LUON duoc update)

- VAN DE: modules/proxy_providers/ipv6_provider.py KHONG BAO GIO duoc update tren VM
  * Du da fix updater + __pycache__ cleanup → file van cu
  * Firewall code nam trong file khong duoc deploy → Chrome van dung IPv4
- FIX: Copy firewall code vao modules/drission_flow_api.py
  * File nay NAM TRUC TIEP trong modules/ → updater LUON copy
  * _block_ipv4_for_chrome(): Block IPv4 TCP+UDP cho Chrome Portable
  * _unblock_ipv4_for_chrome(): Go rules khi close()
  * _get_chrome_paths(): Tim Chrome exe trong GoogleChromePortable/
  * Goi tu setup_and_open_chrome() khi proxy provider tra "" (direct mode)
- File: modules/drission_flow_api.py

v1.0.617
2026-03-31 (VN)
Fix: VM IPv6 DIRECT mode + firewall block IPv4 (da test logic OK)

- Quay lai direct mode (khong SOCKS5 vì cham)
- Logic firewall da verify: tim 4 Chrome exe, tao 8 rules (TCP+UDP)
- Commands dung format: protocol=tcp/udp remoteip=0.0.0.0-255.255.255.255
- File: modules/proxy_providers/ipv6_provider.py
