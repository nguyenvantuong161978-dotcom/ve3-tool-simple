1.0.134
2026-02-26 (VN)
Fix: _wait_for_textarea_visible() hỗ trợ contenteditable

Thay đổi:
- Verification step dùng _find_textarea() thay vì chỉ tag:textarea
- Hỗ trợ cả giao diện cũ (textarea) và mới (contenteditable div)
- Không bị timeout chờ textarea khi Flow dùng contenteditable

1. v1.0.134: Fix verification trong _wait_for_textarea_visible()
2. v1.0.133: Click "Quay lại dự án" sau Create with Flow
2. v1.0.132: PRE-LOGIN dùng cùng logic chọn project
2. v1.0.131: Visit Flow sau login để set cookies
3. v1.0.130: Không xóa Chrome data 2 lần
2. v1.0.129: Check logout ngay từ đầu
3. v1.0.128: Thêm click "Create with Flow" khi mồi
4. v1.0.127: Hoàn thiện hỗ trợ giao diện Flow mới
2. v1.0.126: Cập nhật cho giao diện Flow mới
2. v1.0.125: 2FA thử Ctrl+V trực tiếp trước
2. v1.0.124: 2FA dùng Ctrl+V (revert direct input)
3. v1.0.123: Login tuần tự Chrome 1 → Chrome 2
4. v1.0.122: 2FA login cải thiện (multiple selectors)
5. v1.0.121: PRE-LOGIN trong Chrome workers (revert CMD riêng)
2. v1.0.120: Fix PRE-LOGIN dùng Popen + CREATE_NEW_CONSOLE + sys.executable
3. v1.0.119: Fix PRE-LOGIN dùng subprocess.call + cmd.exe /c start
4. v1.0.118: Fix PRE-LOGIN CMD dùng os.system (thay vì subprocess)
5. v1.0.117: Fix PRE-LOGIN dùng machine_code từ folder path
6. v1.0.116: Fix PRE-LOGIN CMD visible (CREATE_NEW_CONSOLE)
7. v1.0.115: PRE-LOGIN chạy như CMD riêng (visible)
8. v1.0.114: PRE-LOGIN trong GUI trước khi start workers
2. v1.0.113: Login cả 2 Chrome trước khi vào Flow
3. v1.0.112: Login Google cả khi RESUME với 0% ảnh
4. v1.0.111: Đăng nhập Google trước khi vào Flow
5. v1.0.110: Fix tìm mã máy đầy đủ trong sheet (AR8-T1)
2. v1.0.109: Fix GUI Settings bị che + tự tắt
3. v1.0.108: GUI select account (Listbox + chọn tài khoản)
4. v1.0.107: Clear Chrome data on NEW project (not resume)
5. v1.0.106: Save account to Excel for proper resume
6. v1.0.105: Multi-account rotation per channel (sheet THONG TIN)
2. v1.0.104: Prioritize projects by completion % (highest first)
3. v1.0.103: Delete child rows from Excel before loading prompts
3. v1.0.102: Fix skip child ref images on resume (is_child in _load_prompts)
4. v1.0.101: Auto 2FA login support (TOTP)
3. v1.0.100: Fix GUI "Da xong" button copy check
3. v1.0.99: Change completion threshold 95% -> 80%
4. v1.0.98: Fix Chrome 2 infinite retry + Allow 95% completion
5. v1.0.97: Add INSTALL.bat for new machine setup
6. v1.0.96: Add gspread + google-auth to requirements.txt
7. v1.0.95: Fix skip child reference images on resume
8. v1.0.94: Delete local folder - stronger retry logic
9. v1.0.93: Auto-detect IPv6 on START
10. v1.0.92: Fix IPv6 file name compatibility
