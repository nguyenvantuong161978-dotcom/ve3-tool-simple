1.0.635
2026-03-31 (VN)
Fix: Server Chrome - detect IPv6 chet qua proxy connect failures, rotate NGAY thay vi doi 403
- VAN DE: IPv6 chet sau setup nhung worker chi rotate khi gap 403
  → 429/400 do IPv6 chet bi xu ly sai: switch model hoac skip task
  → Chrome chay 5-10 phut voi IPv6 chet, skip nhieu task truoc khi rotate
- FIX 1: ipv6_proxy.py track _connect_failures (tang khi fail, reset khi success)
- FIX 2: chrome_pool.py check proxy connect failures khi gap 429/400
  → Neu >= 5 connect failures → IPv6 chet → rotate IPv6 + restart (nhu 403)
  → Task duoc re-queue thay vi skip/fail
- Ket qua: Rotate trong <30s thay vi 5-10 phut, giam mat task

v1.0.634
2026-03-31 (VN)
Fix: Server video - khong bat buoc Lower Priority model, interceptor thay model tu VM
- Server Chrome chi can o Video mode (chon model bat ky co san)
- Interceptor thay videoModelKey tu payload VM gui (VD: lower priority)
- Tai khoan server khong can co Lower Priority, chi can VM co quyen la du
- Vi interceptor thay bearer token → Google check quyen cua VM, khong phai server

v1.0.633
2026-03-31 (VN)
Fix: Server worker - IPv6 mat mang sau setup → rotate TRUOC khi retry
- VAN DE: Chrome-1 IPv6 pass test luc setup nhung mat ket noi ngay sau
  → Chrome load Flow page 20 lan fail, worker khong bao gio ready
  → Retry setup nhung IPv6 van chet → worker dung hoan toan
- FIX 1: Rotate IPv6 TRUOC khi retry setup (truoc: rotate SAU khi fail)
- FIX 2: Kill Chrome cu truoc khi retry (tranh chiem port + dung IPv6 cu)
- FIX 3: Close session cu trong _setup_single_worker khi retry

v1.0.632
2026-03-31 (VN)
Fix: Server Chrome video - THUC SU chuyen sang Video mode (khong con FORCE MODE)
- Truoc: Server Chrome o Image mode, interceptor doi URL sang video → Google reject (recaptcha sai mode)
- Sau: Click chuyen sang Video mode thuc su (Video tab → Thanh phan → 16:9 → x1 → Lower Priority)
- Interceptor chi thay token + projectId + inject mediaId (KHONG doi URL)
- Chrome gui VIDEO request voi recaptchaToken hop le cho video mode
- Giong cach VM Chrome lam (JS_SWITCH_TO_T2V_MODE trong drission_flow_api.py)

v1.0.631
2026-03-31 (VN)
Feature: Video generation (I2V) ho tro SERVER MODE
- Truoc: _video_worker_loop LUON dung Chrome local (drission_api) → server mode khong tao video
- Fix: Check generation_mode, neu 'server'/'api+server' → dung GoogleFlowAPI gui qua server
- Server Chrome dung FORCE MODE (JS interceptor) tao video
- Flow: VM gui media_id → server /api/fix/create-video-veo3 → operations → VM poll Google lay video

v1.0.630
2026-03-31 (VN)
Fix: IPv6 Pool GUI bi dong/treo khi rotate_ip dang chay
- Root cause: _refresh_ui() chay tren main thread, goi pool.get_status() can _lock
- Khi rotate_ip() giu _lock (MikroTik API 2-5s+) → main thread block → GUI dong
- Fix: Fetch pool data trong background thread, chi update widget tren main thread

v1.0.629
2026-03-31 (VN)
Feature: Server Chrome tao video tu anh (I2V - Image to Video)
- Endpoint moi: POST /api/fix/create-video-veo3
- Server Chrome dung FORCE MODE: o image mode, interceptor convert sang video request
- VM gui media_id (tu anh da tao) → server tra operations → VM poll Google lay video
- Timeout cho anh tang 120s → 180s

v1.0.628
Fix: Server worker loop - auto rotate IPv6 khi setup fail (IPv6 mat mang sau test ban dau)
- Khi worker setup fail → rotate IPv6 qua pool client → re-setup proxy → retry
- Xu ly truong hop IPv6 pass test luc dau nhung mat ket noi sau do

v1.0.627
Fix: Server IPv6 - test internet thuc te (curl Google) thay vi chi ping gateway
- Neu IPv6 khong co internet → auto rotate sang IPv6 moi tu pool
- Truoc: chi ping gateway (local) → "OK" nhung Chrome khong ket noi duoc

v1.0.626
Fix: Token refresh 401 - dung DrissionPage thay PyAutoGUI (F12+JS khong hoat dong)

v1.0.625
Fix: IPv6 API server bi dong/treo - doi HTTPServer thanh ThreadingHTTPServer

v1.0.624
Add: ve3/ folder vao danh sach update (ZIP/manual) de may khac cap nhat duoc

v1.0.623
Fix: Login - doi URL chuyen sang password page + nhieu selector

- VAN DE: input[type="password"] khong tim thay sau 15s
  * Sau khi nhap email, code chi sleep(3) roi tim password input
  * Mang cham → trang chua chuyen sang /challenge/pwd → khong co input
  * Chi tim 1 selector: input[type="password"] → Google doi thi fail
- FIX 1: Doi URL chuyen sang /challenge/pwd truoc (max 20s)
  * Thay sleep(3) bang loop check URL
  * Detect "challenge/pwd" hoac "challenge/password"
- FIX 2: Thu nhieu selector thay vi chi 1
  * input[type="password"], input[name="Passwd"], input[name="password"]
  * input[autocomplete="current-password"], #password input, div[id="password"] input
  * Max 20s (tang tu 15s)
- File: google_login.py

v1.0.622
2026-03-31 (VN)
Fix: Pool lay qua nhieu IP - register sai format + khong release IP cu

- BUG 1: register_ip so sanh "3065" vs "65" → LUON fail → luon get_ip moi
  * Pool luu subnet_hex = "65" (2 ky tu)
  * register extract = "3065" (4 ky tu) → khong match
  * FIX: Luon lay 2 ky tu cuoi: parts[3][-2:]
- BUG 2: VM khong release IP cu truoc khi lay IP moi
  * Register fail → get_ip → IP cu van "in_use" mai
  * 2 VM × nhieu lan restart = chiem 10+ IP
  * FIX: Release IP cu truoc get_ip
- BUG 3: release_ip chi match exact address
  * Sau RESET, pool co IP moi cho cung subnet → exact match fail
  * FIX: Fallback match theo subnet_hex
- Files: ipv6/ipv6_pool.py, modules/ipv6_rotator.py

v1.0.621
2026-03-31 (VN)
Fix: Login fail khi mang cham - doi element thay vi sleep co dinh

- VAN DE: Code dung time.sleep(3) co dinh → mang cham thi trang chua load xong
  * Password input chua xuat hien → Ctrl+V paste vao void
  * OTP input chua xuat hien → OTP khong duoc nhap
  * Chi check 5 lan (10s) → timeout qua ngan
- FIX: Doi element xuat hien thay vi sleep:
  * Password: doi input[type="password"] xuat hien (max 15s)
  * 2FA page: doi URL chuyen sang "challenge" (max 10s)
  * OTP: doi input[type="tel"] hoac #totpPin xuat hien (max 15s)
  * Click vao input truoc khi paste (dam bao focus)
  * Tang login check 5→10 lan (20s thay vi 10s)
- File: google_login.py

v1.0.620
2026-03-31 (VN)
Fix: Firewall bi treo 80s - bo duplicate + check exists truoc khi tao

- VAN DE: Firewall chay 2 LAN (ipv6_provider + drission_flow_api)
  * Delete 20 rules × 5s timeout = 100s cho
  * Tao lai 8 rules = them 20s
  * Tong: 80-120s bi "dong" moi lan mo Chrome
- FIX 1: Bo firewall trong ipv6_provider.py (da chuyen sang drission_flow_api.py)
- FIX 2: Check rules da ton tai → skip (khong delete/recreate)
- FIX 3: Giam range 20→8, timeout 5s→3s
- Ket qua: Tu 80s → <1s (neu rules da co)
- Files: modules/drission_flow_api.py, modules/proxy_providers/ipv6_provider.py

v1.0.619
2026-03-31 (VN)
Fix: Block IPv4 TRUOC khi mo Chrome (google_login.py)

- VAN DE: Firewall code chi chay trong DrissionFlowAPI.setup_and_open_chrome()
  * google_login.py mo Chrome RIENG (khong qua DrissionFlowAPI)
  * Chrome login mo TRUOC → da ket noi IPv4 → firewall sau vo nghia
- FIX: Goi _block_ipv4_for_chrome_static() trong google_login.py
  * Block IPv4 TRUOC KHI mo Chrome
  * Chrome mo ra da khong the dung IPv4
  * Static method: goi duoc tu bat ky dau (khong can instance)
- Files: google_login.py, modules/drission_flow_api.py

v1.0.618
2026-03-31 (VN)
Fix: Di chuyen firewall block IPv4 vao drission_flow_api.py (file LUON duoc update)

- VAN DE: modules/proxy_providers/ipv6_provider.py KHONG BAO GIO duoc update tren VM
  * Du da fix updater + __pycache__ cleanup → file van cu
  * Firewall code nam trong file khong duoc deploy → Chrome van dung IPv4
- FIX: Copy firewall code vao modules/drission_flow_api.py
  * File nay NAM TRUC TIEP trong modules/ → updater LUON copy
  * _block_ipv4_for_chrome(): Block IPv4 TCP+UDP cho Chrome Portable
  * _unblock_ipv4_for_chrome(): Go rules khi close()
  * _get_chrome_paths(): Tim Chrome exe trong GoogleChromePortable/
  * Goi tu setup_and_open_chrome() khi proxy provider tra "" (direct mode)
- File: modules/drission_flow_api.py

v1.0.617
2026-03-31 (VN)
Fix: VM IPv6 DIRECT mode + firewall block IPv4 (da test logic OK)

- Quay lai direct mode (khong SOCKS5 vì cham)
- Logic firewall da verify: tim 4 Chrome exe, tao 8 rules (TCP+UDP)
- Commands dung format: protocol=tcp/udp remoteip=0.0.0.0-255.255.255.255
- File: modules/proxy_providers/ipv6_provider.py
