1.0.568
2026-03-30 (VN)
Change: Privacy Extension (RFC 4941) - IP moi giong IP that cua OS

- BUG: Pool chi load 3-6 IP (min_pool_size) thay vi tat ca 155 IP trong range
  * _refill_if_needed() chi them khi available < min_pool_size (3)
  * _refill() bi gioi han boi max_pool_size (20)
  * Ket qua: Pool chi hien 6 IP, khong thay het kho IPv6
- FIX: Them _load_all_from_router() - load TAT CA IP trong allowed range
  * Scan router → lay moi IP trong subnet 65-ff chua co trong pool
  * Khong gioi han max_pool_size (pool la kho, can thay het)
  * Skip IP da burned hoac da co trong pool
  * Chay 1 lan khi init() → sau do dung refill binh thuong
- File: ipv6/ipv6_pool.py

1.0.562
2026-03-30 (VN)
Change: VM GUI IPv6 Pool + Thong ke API + Doi IPv6 thu cong

- VM Setup popup: Them loai proxy "IPv6 Pool" (MikroTik Pool API)
  * Nhap Pool API URL + TEST KET NOI (hien thi so IP san sang/dang dung/burned)
  * DOI IPv6 THU CONG: Lay IP tu pool va set len may (test nhanh)
  * LUU CAU HINH: Luu pool_api_url vao mikrotik section trong settings.yaml
  * Auto-detect: Kiem tra Pool API truoc, fallback sang file ipv6.txt
- IPv6 GUI: Them section "Thong ke API"
  * Hien thi: Lay IP / Doi IP / Burn / Tra lai (dem so lan)
  * Worker tracking: Worker nao dang dung IP nao, bao nhieu lan get/rotate/burn
  * Auto-refresh stats moi 10 giay khi API dang chay
- API Server: Them /api/stats endpoint + tracking tat ca actions
  * _track_stat(): Dem get/rotate/burn/release theo tong va theo worker
  * get_api_stats(): Thread-safe access thong ke
  * /api/status: Bao gom api_stats trong response
- Files: vm_manager_gui.py, ipv6/ipv6_server.py, ipv6/ipv6_gui.py

1.0.561
2026-03-30 (VN)
Change: Server IPv6 Pool - Server tu dong lay/doi IPv6 qua Pool API cho moi Chrome worker

- Server mode: ChromePool ket noi Pool API de lay IPv6 cho moi Chrome
  * setup_pool_client(): Ket noi Pool API server (vd: http://192.168.88.1:8765)
  * init_workers(): Tu dong get_ip() cho moi worker neu chua co IPv6
  * 403 recovery: Dung rotate_pool_ip() thay vi static list (get_next_ipv6)
  * close_all(): Release tat ca IPv6 ve pool khi dong server
- Backward compat: Khong co Pool API → van dung static IPv6 list nhu cu
- Config: Doc pool_api_url tu server_gui.json hoac fallback settings.yaml
- Server GUI: Them truong "IPv6 Pool API" trong setup page
- Files: server/chrome_pool.py, server/app.py, server/server_gui.py

1.0.560
2026-03-30 (VN)
Change: Tich hop IPv6 Pool vao ipv6_rotator - VM tu dong lay/doi IPv6 qua Pool API khi co config

- MikroTik www-ssl can certificate → nhieu router chua cau hinh
- FIX: Thu HTTPS truoc, neu fail tu dong thu HTTP
- Ap dung cho: test_pool.py (--detect) va mikrotik_api.py (tat ca API calls)
- Huong dan bat www: /ip/service/set www disabled=no
- Files: ipv6/test_pool.py, ipv6/mikrotik_api.py

1.0.553
2026-03-30 (VN)
Change: IPv6 AUTO DETECT - tu dong lay interface/prefix/subnet tu MikroTik router

- Them --detect mode: chi can host + username + password
  * Ket noi router → lay tat ca IPv6 → phan tich prefix + interface + subnet range
  * Hien thi tat ca IPv6 (global vs link-local, dynamic, invalid)
  * Goi y config toi uu (tranh trung subnet dang dung)
  * Hoi luu vao config_test.json
- Usage: python -m ipv6.test_pool --detect
- File: ipv6/test_pool.py

1.0.552
2026-03-30 (VN)
Change: FIX UPDATE thieu thu muc ipv6/ - them copy ipv6/ khi update qua ZIP

- BUG: Nut UPDATE (ZIP mode) chi copy modules/ va server/ → thieu ipv6/
  * May khong co Git → tai ZIP → giai nen → chi copy file cu
  * Thu muc ipv6/ (IPv6 Dynamic Pool) khong duoc copy sang
- FIX: Them copy ipv6/ folder trong ca 2 GUI:
  * vm_manager_gui.py: Copy ipv6/*.py khi update
  * server/server_gui.py: Copy ipv6/*.py khi update
- Files: vm_manager_gui.py, server/server_gui.py

1.0.551
2026-03-30 (VN)
Change: THUMB cho server - Chrome 2 doi server san sang thay vi bo qua

- BUG: Chrome 2 tao thumbnail → 3 server chua san sang → bo qua luon!
  * pool.refresh_all() → 0 available → return False ngay
  * Thumbnail khong duoc tao, phai doi cycle sau
- FIX: Server mode → CHO VO HAN den khi co server (giong Chrome 1)
  * Khi khoi dong: Cho server san sang (retry moi 30s + refresh)
  * Khi tao tung thumb: Cho server san sang (retry moi 60s + refresh)
  * Log: "[THUMB] Chua co server san sang - cho den khi co server..."
  * Non-server mode: Giu nguyen logic cu (return False ngay)
- File: modules/smart_engine.py - _generate_thumbnails_via_server()

1.0.550
2026-03-30 (VN)
Change: FIX VM mat anh - Nhan biet task FAILED tren server ngay + retry cung server

- BUG (CRITICAL): Server tra "task failed" nhung VM cu polling 10 phut roi moi timeout!
  * Server: {"success": false, "error": "ContextLostError..."} (HTTP 200)
  * VM _poll_proxy_task(): if not result.get("success") → continue (POLL TIEP!)
  * VM khong doc error message → khong biet task da that bai → cho 10 phut stuck timeout
  * Ket qua: Server tao 2 anh, 1 fail → VM mat 10 phut cho task fail → chi nhan 1 anh
- FIX 1: _poll_proxy_task - detect server failure NGAY
  * Check result.get("error") khi success=False
  * Co error → return False ngay (khong poll tiep)
  * Khong co error → van poll (intermediate state)
- FIX 2: _poll_proxy_video_task - tuong tu cho video
- FIX 3: Retry CUNG SERVER khi task failed (khong phai timeout)
  * Truoc: _allow_same chi cho refs → scenes voi 1 server KHONG retry duoc
  * Sau: Server mode + task failed → cho retry cung server (lan cuoi)
  * ContextLostError, 403, etc = co the retry → OK
  * Timeout = khong retry (tranh duplicate) → giu nguyen
- FIX 4: JS_INTERCEPTOR trong drission_flow_api.py
  * Cung bug nhu server: IIFE khong co return → DrissionPage nhan None
  * Them "return" truoc (function(){...})()
- Flow moi khi server task fail:
  Server tra failed → VM detect NGAY (< 2s) → retry server khac/cung → tao lai anh
  (Truoc: Server tra failed → VM cho 10 phut → timeout → prompt retry cycle sau)
- Files: modules/google_flow_api.py, modules/browser_flow_generator.py, modules/drission_flow_api.py

1.0.549
2026-03-30 (VN)
Change: FIX 401 Token Expired - VM tu dong refresh token khi server tra 401

- VAN DE: VM gui token cu (het han sau 1h) → server dung dung token do → Google tra 401
  * Server mode khong co logic refresh token (chi API mode co)
  * Server khong phan biet 401 vs loi khac → retry cung token cu → 401 vo han
- FIX VM (_generate_via_local_server):
  * Detect 401 tu error message ("401", "authentication", "unauthenticated")
  * Goi _auto_extract_token(force_refresh=True) → mo Chrome lay token moi
  * Retry voi token moi tren server khac
  * Cap nhat bearer_token (nonlocal) de cac prompt sau dung token moi
- FIX SERVER (chrome_pool.py):
  * Phan biet 401 rieng (truoc 403/400/429)
  * 401 → mark failed NGAY, KHONG retry (retry vo ich vi token cu)
  * Log ro: "TOKEN HET HAN - VM can refresh"
- Flow moi khi 401:
  Server tra 401 → VM detect → mo Chrome lay token moi → retry voi token moi → OK
- Files: modules/browser_flow_generator.py, server/chrome_pool.py

1.0.548
2026-03-30 (VN)
Change: VM PERSISTENT POLLING - Server mode KHONG BAO GIO bo cuoc

- VAN DE: VM hay bo cuoc khi mode=server:
  * _poll_proxy_task: Hard timeout 600s → task dang lam tren server cung bi cancel
  * wait_for_server(300): Chi cho 5 phut → khong co server = FAIL ngay
  * Submit timeout → danh dau failed ngay (trong khi server co the dang lam)
  * num_servers=0 → return FAIL (khong cho server khoi dong)
- FIX: Khi generation_mode='server' hoac 'api+server':
  * _poll_proxy_task: BO hard timeout 600s → chi timeout khi STUCK (10 phut khong thay doi)
  * Task 404 (server restart) → return de retry (khong loop vo han)
  * Track consecutive errors → 30 loi lien tiep moi bo cuoc
  * wait_for_server: CHO VO HAN thay vi 5 phut (retry moi 60s + refresh servers)
  * Submit timeout: KHONG tang failed_count → prompt retry o cycle sau
  * Retry: Tang len max(len(servers), 3) thay vi 1
  * num_servers=0: CHO den khi co server (retry moi 30s)
- KET QUA:
  * Server mode: VM gui task → cho server lam xong → KHONG BAO GIO bo cuoc
  * Task con tren server → cho vo han
  * Server chet → retry server khac / cho server phuc hoi
  * Khong co server → cho den khi co
- Files: modules/google_flow_api.py, modules/browser_flow_generator.py

1.0.547
2026-03-30 (VN)
Change: FIX Server 403 loop + sleep negative bug

- BUG 1 (CRITICAL): Server 403 loop vo han - Interceptor JS KHONG inject duoc
  * Log: "Interceptor: None" = JS injection that bai
  * Server Chrome dung TOKEN CUA CHINH NO thay vi token tu VM
  * Token khong match projectId → 403 MOI LAN
  * Doi IPv6 + fingerprint + account VO ICH vi van de la token SAI
  * Lang phi IPv6 addresses (moi retry dot 1 IP)
  * FIX: Retry interceptor injection 3 lan + verify window.__proxyInterceptReady
  * FIX: Neu interceptor fail → return error NGAY (khong gui request voi token sai)
  * FIX: Tang sleep 0.5→1s cho page xu ly
  * FIX: chrome_pool.py: Interceptor fail → re-queue task + restart Chrome (KHONG doi IPv6)
- BUG 2: "sleep length must be non-negative" crash
  * server_pool.py: wait_for_server() tinh sleep time bi am khi vuot max_wait
  * FIX: Check remaining <= 0 → break ngay
- Files: server/chrome_session.py, server/chrome_pool.py, modules/server_pool.py

1.0.545
2026-03-29 (VN)
Change: PROXY PROVIDER PLUGGABLE - He thong proxy module hoa (IPv6 / Webshare / None)

- Tao modules/proxy_providers/ - he thong proxy module hoa:
  * base_provider.py: Abstract interface (setup, rotate, get_chrome_arg, stop)
  * ipv6_provider.py: Wrap IPv6 rotator + SOCKS5 proxy hien tai
  * webshare_provider.py: Webshare.io Rotating Residential (doi IP bang session ID)
  * __init__.py: Factory create_provider() + NoneProvider + backward compat
- Config format (settings.yaml):
  * proxy_provider.type: "none" | "ipv6" | "webshare"
  * proxy_provider.webshare: {rotating_username, rotating_password, machine_id}
  * Backward compat: ipv6_rotation.enabled van hoat dong
- Tich hop Server mode:
  * chrome_session.py: Dung ProxyProvider neu co, fallback IPv6 truc tiep
  * chrome_pool.py: setup_proxy_providers() tao provider cho moi worker
  * app.py: Doc proxy_config tu server_settings, truyen cho ChromePool
- Tich hop VM mode:
  * drission_flow_api.py: _proxy_provider uu tien hon IPv6 truc tiep
  * _rotate_ipv6() dung ProxyProvider.rotate() neu co
- GUI Settings:
  * server_gui.py: Dropdown proxy type + Webshare fields + TEST button
  * vm_manager_gui.py: Setup VM popup - Proxy type selector + Webshare/IPv6 fields
  * Show/hide fields theo loai proxy, luu proxy_provider section vao settings.yaml
- Webshare Rotating Residential:
  * Local HTTP proxy bridge (WebshareProxyBridge) xu ly CONNECT tunnel + auth
  * Doi IP bang tang session_id trong username (~1s, nhanh hon IPv6)
  * Chrome → localhost:{port} → p.webshare.io:80 → Target

1.0.544
2026-03-29 (VN)
Change: Server TIMEOUT RETRY - Flow bi do/load cham thi thu lai tai cho (khong tinh fail)

- VAN DE: Flow tao anh 99% xong nhung bi do/load cham → timeout 120s → tinh fail
  * Khong phai 403, khong phai loi that - chi la Flow bi do
  * VM nhan "Timeout 120s" → coi nhu fail → retry tren server khac (lang phi)
  * Anh gan xong bi bo, phai tao lai tu dau
- FIX: Server tu retry TAI CHO khi timeout (toi da 3 lan)
  * Timeout → doi 3s → reload page → gui lai prompt → doi response
  * Chi retry khi TIMEOUT (khong retry 403/400/loi khac)
  * Lan 1: timeout → retry | Lan 2: timeout → retry | Lan 3: timeout → fail
  * Log: "[TIMEOUT RETRY] Flow bi do → thu lai lan 2/3..."
  * Thanh cong: "[TIMEOUT RETRY] OK sau 2 lan!"
- File: server/chrome_session.py - generate_image()

1.0.543
2026-03-29 (VN)
Change: FIX thumbnail dung ServerPool - smart server selection + retry tat ca servers

- BUG: Thumbnail dung SINGLE server URL (cfg.local_server_url) truc tiep
  * Server 146 timeout → tat ca 3 portrait thumbnails FAIL
  * Khong co retry, khong co smart selection, khong check chrome_ready
  * Log: "[THUMB] LOCAL SERVER MODE: http://192.168.88.146:5000" → timeout x3
- FIX: Dung ServerPool giong nhu image generation chinh
  * pick_best_server(): Chon server nhanh nhat, it queue nhat
  * Retry tat ca servers khi fail (thumbnail idempotent nhu refs)
  * Track timeout count de deprioritize server cham
  * Requires chrome_ready + successful refresh (v1.0.542 logic)
- File: modules/smart_engine.py - _generate_thumbnails_via_server()

1.0.542
2026-03-29 (VN)
Change: FIX server selection - PHAI refresh OK + retry TAT CA servers

- BUG 1: Server chua bat nhung van hien "OK" → gui task → TIMEOUT
  * Truoc: Chua refresh (last_check=0) → coi nhu available (chua biet)
  * Sau: PHAI refresh thanh cong moi available
  * Refresh fail → chrome_ready=False → khong nhan task
- BUG 2: Retry chi thu 1 server khac → 2/3 server hong = fail
  * VD: nv6 → 146 TIMEOUT → retry 165 TIMEOUT → FAIL (khong thu 145!)
  * Sau: Refs retry TAT CA servers (loop qua tung server)
  * Scenes van retry 1 lan (tranh duplicate)
  * Log: "[RETRY 1/3] → server2", "[RETRY FAIL] server2", "[RETRY 2/3] → server3"
- FIX 3: Refresh fail → mark chrome_ready=False (khong de mac dinh)
- FIX 4: Tang join timeout 5s → 10s (cho server cham)
- v1.0.541: Response time + timeout tracking (van giu)

1.0.540
2026-03-29 (VN)
Change: FIX trung ID nvc trong Excel + Chrome 2 chap nhan 80% media_id

- BUG 1: API tra ve 2 narrator (35 tuoi + 23 tuoi) → ca 2 duoc gan ID "nvc"
  * Chrome 1 chi tao 1 anh nvc.png → dong 2 khong co media_id
  * Chrome 2 doi media_id cho dong 2 → doi mai
  * FIX: Chi narrator DAU TIEN la "nvc", cac narrator sau dung nv{counter}
  * File: modules/progressive_prompts.py - them nvc_used flag

- BUG 2 (v1.0.539): Chrome 2 doi 100% media_id → 1 ref fail = block tat ca
  * FIX: Chap nhan 80% media_id (vd: 8/10 ref co media_id → OK, chay luon)
  * Neu 1-2 ref bi fail/timeout, Chrome 2 van tao scenes duoc
  * File: _run_chrome2.py - media_ratio >= 0.8

- v1.0.539: Deduplicate NV theo unique ID (van giu)

1.0.537
2026-03-29 (VN)
Change: FIX ref TIMEOUT infinite loop - refs retry tren server khac khi timeout

- BUG: Ref (nv/loc) chi co 1 prompt → gui server → TIMEOUT → STOP (thieu media_id)
  * FULL RESTART → gui LAI cung server → TIMEOUT → STOP → loop vo han
  * VM mac ket, khong tien trien duoc
  * Root cause: TIMEOUT = KHONG retry (tranh duplicate) → dung cho scenes, SAI cho refs
- FIX: Refs (nv/loc) la idempotent → AN TOAN retry khi TIMEOUT
  * Scene TIMEOUT: Van KHONG retry (tranh duplicate trong queue) - giu nguyen
  * Ref TIMEOUT: Fall through vao retry logic, thu server khac
  * Neu chi co 1 server: Cho phep retry CUNG server (doi 10s truoc khi retry)
  * Refs tao lai khong sao (cung prompt → cung ket qua, va duplicate ref khong anh huong)

1.0.536
2026-03-29 (VN)
Change: REUSE project - retry reload 3 lan truoc khi tao moi

- Truoc: Reuse fail (textarea khong co) → tao project moi ngay (mat 30-60s)
- Sau: Thu reload project cu 3 lan truoc:
  * Reload URL + dismiss popup + doi textarea (15s moi lan)
  * Neu 3 lan van fail → moi tao project moi
  * Tiet kiem thoi gian khi project cu chi can F5

1.0.535
2026-03-29 (VN)
Change: Mode API = LUON dung Chrome local, KHONG gui qua server

- Truoc: Chon mode API nhung co Server URLs → tu dong gui qua server (SAI!)
  * User chon API la muon dung Chrome local (DrissionPage)
  * Nhung code thay local_server_enabled=True → gui server thay Chrome
- Sau: Mode API = luon dung Chrome local
  * Tam tat local_server_enabled khi chay API mode
  * Server URLs chi dung cho mode 'server' va 'api+server'
  * User chon gi duoc nay: API=Chrome, Server=Server, API+Server=ca hai

1.0.534
2026-03-29 (VN)
Change: FIX REUSE project fallback - tao project moi khi textarea khong xuat hien

- BUG: Reuse project URL cu → textarea khong xuat hien → FAIL!
  * Fallback check '/project/' not in URL → URL da co '/project/' → skip tao moi
  * Ket qua: worker CHET, khong phuc hoi duoc
- FIX: Khi reuse fail (textarea khong co):
  * Bo project_url cu (khong dung duoc)
  * Navigate ve Flow home (FLOW_URL)
  * Tao project MOI tu dau
  * Worker phuc hoi binh thuong

1.0.533
2026-03-29 (VN)
Change: STOP scenes khi refs thieu media_id - doi cycle sau

- Truoc: Refs fail (timeout/error) → warn roi chay scenes KHONG CO tham chieu
  * Scenes tao ra KHONG co reference nhan vat/dia diem → chat luong kem
  * Phai tao lai scenes sau khi co refs → lang phi thoi gian + quota
- Sau: Refs chua du media_id → DUNG LAI, khong tao scenes
  * Doi cycle tiep theo khi refs da co media_id
  * Dam bao scenes LUON co day du tham chieu
  * Log ro rang: "[STOP] X/Y refs THIEU media_id!"

1.0.532
2026-03-29 (VN)
Change: Server REUSE project URL + FIX GoogleFlowAPI._log() crash

- SERVER REUSE PROJECT URL (tiet kiem thoi gian):
  * Truoc: MOI LAN restart Chrome (403 → fingerprint moi) → tao project moi (~30-60s)
  * Sau: Restart giu data → navigate thang vao project URL cu (3-5s)
  * Chi tao project moi khi: (1) lan dau setup, (2) sau clear data + login lai
  * Neu project cu khong con (URL loi/het han) → tu dong tao project moi
  * clear_data=True → reset project_url (can tao moi sau login)
- FIX: GoogleFlowAPI._log() crash - them level parameter
  * _log() chi nhan 1 arg nhung co cho goi voi 2 args (message, level)
  * Server 145 tra ve loi: "takes 2 positional arguments but 3 were given"
  * Them level parameter: _log(self, message, level="INFO")

1.0.531
2026-03-29 (VN)
Change: FIX servers bi DOWN oan do refresh_status + tang refresh timeout

- BUG: refresh_status() timeout → tang connect_fail_count → server bi DISABLED
  * Server dang ban (7 VMs gui cung luc) → /api/status cham → refresh fail
  * Moi refresh fail += 1 → 5 lan = server DOWN 60s
  * Nhung server KHONG CHET - chi ban/cham thoi!
- FIX: refresh_status() KHONG tang connect_fail_count
  * Refresh chi la kiem tra, KHONG PHAI submit that
  * Chi mark_submit_failed() (submit that bai) moi disable server
  * Server ban → refresh fail → van available → VM gui duoc
- Tang refresh timeout: 5s → 10s (server ban can nhieu thoi gian hon)

1.0.530
2026-03-29 (VN)
Change: FIX duplicate images - submit timeout KHONG retry + tang timeout 60s

- BUG: Submit timeout bi phan loai sai thanh CONNECT error → retry tren server khac
  * Server co the DA NHAN task (chi response cham) → retry tao DUPLICATE
  * Server lam 2 anh cho 1 prompt, VM chi nhan 1 → LECH so luong
- FIX: Submit timeout = KHONG retry (task co the da nam trong queue)
  * "Proxy request timeout" → mark_task_failed → return False (khong retry)
  * Prompt se duoc retry o vong scan tiep theo (khong mat)
- Tang submit timeout: 30s → 60s (giam chance timeout khi server ban)
- Phan loai loi 3 loai:
  * TIMEOUT: submit cham → KHONG retry (tranh duplicate)
  * CONNECT: server chet → retry server khac (ok vi task chua tao)
  * TASK: Google API loi → retry server khac

1.0.529
2026-03-29 (VN)
Change: Server slot reservation - moi server chi nhan 1 prompt/lan tu VM

- local_pending: Track so prompt VM dang gui cho moi server
  * pick_best_server() tu dong reserve (+1 pending)
  * mark_success/mark_task_failed/mark_submit_failed tu dong release (-1 pending)
  * Sort theo (queue_size + local_pending) → server it viec nhat
- Tranh qua tai: 3 server → 3 thread → moi thread 1 server rieng
  * Thread 1 pick server A (+1) → Thread 2 thay A.pending=1, chon B
  * Xong prompt → release → thread khac co the chon lai
- release_server(): Giam pending khi task xong ma khong can mark success/fail
- Fix: Retry path release server2 dung khi fail

1.0.528
2026-03-29 (VN)
Change: QUEUE-BASED ServerPool - xep hang cho thay vi disable server

- ServerPool redesign: Task da submit vao server = CHAC CHAN se duoc lam
  * Tach connect_fail (server chet) vs task_fail (Google loi)
  * Chi disable server khi KHONG KET NOI DUOC (5 connect fails)
  * Task fail (Google API error) → KHONG disable server (server van OK)
  * Cooldown giam 300s → 60s (server thuong phuc hoi nhanh)
- Smart polling theo queue_position:
  * Truoc: Fixed 90 polls x 2s = 180s timeout → bo cuoc
  * Sau: Dynamic timeout = 180s + position x 90s (cho theo vi tri)
  * Track position changes: "pos 5 → 3 → 1 → processing → done"
  * Stuck detection: position khong giam 180s → canh bao, 360s → timeout
  * Toi thieu 10 phut cho moi task (du cho queue dai)
- Wait-for-server thay vi give up:
  * Truoc: pick_best_server() = None → failed ngay
  * Sau: wait_for_server(300s) → cho + retry moi 10s → tim server
  * Log: "Cho server... (30s/300s, server gan nhat san sang trong ~45s)"
- Phan loai loi chinh xac:
  * Connection error (timeout, refused) → mark_submit_failed → co the disable
  * Task error (Google API, policy) → mark_task_failed → KHONG disable
  * Backward compat: mark_failed() = mark_task_failed()

1.0.527
2026-03-29 (VN)
Change: FIX CRITICAL - Media_id bi mat do Excel ghi de + ref skip + false warning

- BUG 1 (CRITICAL): Media_ids bi MAT giua cac lan chay (10 → 5 media_ids)
  * ROOT CAUSE: _generate_via_local_server() load workbook 1 lan vao memory (line 3679)
    Nhieu threads share CUNG workbook object qua nonlocal
    Moi thread save → ghi TOAN BO in-memory data ra disk → ghi de thay doi cua Chrome 2
  * FIX: Moi lan save → RELOAD Excel tu disk → update 1 field → save lai
    Dung local _wb = PromptWorkbook() thay vi shared workbook
    Bo nonlocal workbook (khong can share nua)
    Dam bao Chrome 1 va Chrome 2 khong ghi de nhau
- BUG 2: Warning "Khong co reference nao tao thanh cong" khi ref da co file
  * Fix: Phan biet 3 TH: (1) co file+media_id, (2) co file ko media_id, (3) ko file
- BUG 3: _generate_via_local_server() skip ref co file nhung KHONG co media_id
  * Fix: Ref (nv/loc) co file nhung khong media_id → van tao lai qua server

1.0.525
2026-03-29 (VN)
Change: Bo checkbox "BAT Local Server Mode" - chi can nhap URL la du

- Bo checkbox thua: Mode Server/API+Server tu force local_server_enabled
- local_server_enabled tu bat khi co URL, tu tat khi xoa URL
- Text box Server URLs luon enabled (khong can bat checkbox moi nhap duoc)
- GUI gon hon, bot 1 buoc thao tac

1.0.524
2026-03-29 (VN)
Change: FIX UnboundLocalError workbook trong _generate_via_local_server

- BUG: "cannot access local variable 'workbook'" khi save Excel trong thread
  * Python closure: workbook reassign o line 3856 → Python coi la local trong _process_one()
  * Line 3838 truy cap truoc khi assign → UnboundLocalError
- FIX: Them "nonlocal workbook" trong _process_one() (cung voi success_count, failed_count)
- Retry 10 lan da handle duoc (save thanh cong o lan 2) nhung fix de tranh warning

1.0.523
2026-03-29 (VN)
Change: FIX 3 bugs trong server mode flow

- BUG 1 (HIGH): Server mode config restore sai - local_server_url khong restore dung
  * Truoc: Chi restore khi old_url rong (logic nguoc) + thieu restore local_server_list
  * Sau: Luon restore tat ca 3 config values (enabled, url, list)
- BUG 2 (LOW): _auto_extract_token() dung .save() thay vi .safe_save()
  * .save() fail khi Excel bi lock → token mat
  * Doi sang .safe_save() (retry khi lock)
- BUG 3 (LOW): Token chi save khi success==1 (miss khi anh dau skip)
  * Doi sang flag _token_saved_to_excel (save 1 lan duy nhat, bat ke thu tu)
  * Them isinstance check cho bearer_token attribute

1.0.522
2026-03-29 (VN)
Change: Luu bearer token vao Excel sau anh dau tien - server mode doc duoc

- API mode (DrissionPage): Sau anh dau tien thanh cong → luu token + project_id vao Excel
  * _generate_images_drission_mode(): stats.success==1 → set_config_value('flow_bearer_token')
  * _auto_extract_token(): Sau khi lay token → luu vao Excel config sheet
- Truoc: Token chi luu trong memory/cache → server mode khong doc duoc
- Sau: Token luu vao Excel → server mode doc tu Excel → khong can mo Chrome lai
- Flow: API mode tao anh → luu token → chuyen Server mode → doc token tu Excel → chay luon

1.0.521
2026-03-29 (VN)
Change: FIX Server mode - khong can checkbox local_server_enabled + skip Chrome login

- BUG: Mode "Server" bao "chua cau hinh Server URLs" du da nhap URLs
  * has_local_server yeu cau local_server_enabled=True (checkbox cu)
  * Chon radio "Server" nhung checkbox tat → has_local_server=False → fail
- FIX: Mode "server" chi can co URL, khong can checkbox
  * Check (local_server_url or server_list) thay vi has_local_server
  * Force local_server_enabled=True + local_server_url trong config
- FIX: Skip Chrome login khi mode=server (khong can Chrome local)
  * Truoc: Login 2 Chrome + warm-up ~6 phut MOI CYCLE (vo ich)
  * Sau: Skip luon, tiet kiem thoi gian

1.0.520
2026-03-29 (VN)
Change: Them nut "Server" vao Generation Mode - gui anh TRUC TIEP qua server

- GUI: Them radio button "Server" cung cho voi API/Browser/Chrome UI/API+Server
- browser_flow_generator.py: Them handler mode 'server' trong generate_from_prompts_auto()
  * Force local_server_enabled=True, goi generate_from_prompts_api()
  * Bao loi neu chua cau hinh Server URLs
- smart_engine.py: Them 'server' vao tat ca mode checks (4 vi tri)
  * Character generation, Scene generation, Step 5 images, proxy_token bypass
- Server mode = chi dung server, KHONG chay Chrome local
