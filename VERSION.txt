1.0.617
2026-03-31 (VN)
Fix: VM IPv6 DIRECT mode + firewall block IPv4 (da test logic OK)

- Quay lai direct mode (khong SOCKS5 vì cham)
- Logic firewall da verify: tim 4 Chrome exe, tao 8 rules (TCP+UDP)
- Commands dung format: protocol=tcp/udp remoteip=0.0.0.0-255.255.255.255
- File: modules/proxy_providers/ipv6_provider.py

v1.0.615
2026-03-31 (VN)
Fix: Firewall block IPv4 - them import Path (v1.0.614 crash NameError)

- BUG: _block_ipv4_for_chrome() dung Path() nhung KHONG import Path
  * NameError: name 'Path' is not defined → method crash
  * setup() catch exception → firewall rules KHONG duoc tao
  * Chrome van dung IPv4
- FIX: Them `from pathlib import Path` trong _block_ipv4_for_chrome()
- THEM: Log so luong Chrome exe tim thay (de debug)
- File: modules/proxy_providers/ipv6_provider.py

v1.0.614
2026-03-31 (VN)
Fix: Firewall block IPv4 cho Chrome - tach TCP+UDP (netsh khong cho protocol=any+remoteip)

- BUG: netsh tra loi "A specified IP address or address keyword is not valid"
  * protocol=any KHONG dung duoc voi remoteip=0.0.0.0/0
  * Firewall rules KHONG duoc tao → Chrome van dung IPv4
- FIX: Tach thanh 2 rules rieng cho moi Chrome exe:
  * Rule TCP: protocol=tcp remoteip=0.0.0.0-255.255.255.255
  * Rule UDP: protocol=udp remoteip=0.0.0.0-255.255.255.255
  * Moi Chrome = 2 rules → block ca TCP va UDP IPv4
- File: modules/proxy_providers/ipv6_provider.py

v1.0.613
2026-03-30 (VN)
Perf: VM IPv6 DIRECT + Firewall block IPv4 cho Chrome

- VM mode: Bo SOCKS5 proxy → Chrome dung IPv6 truc tiep tren interface
- Firewall block IPv4 outbound cho ChromePortable → Chrome bat buoc dung IPv6
- RDP/system van dung IPv4 binh thuong (chi block Chrome process)
- Server mode: Van dung SOCKS5 proxy (nhieu workers can bind IPv6 khac nhau)
- _block_ipv4_for_chrome(): Add firewall rule block IPv4 khi setup
- _unblock_ipv4_for_chrome(): Go rule khi stop
- google_login.py: VM mode tra "" (khong proxy), dam bao IPv6 tren interface

v1.0.611
2026-03-30 (VN)
Fix: Server IPv6 Pool - firewall TCP/UDP + cleanup static IPs

- Firewall: Mo TCP/UDP outbound cho IPv6 (truoc chi mo ICMPv6 NDP)
  * Ping gateway OK nhung TCP connect fail → thieu outbound TCP rule
- Cleanup static IPs: Xoa 41+ IPv6 static (tu GUI list) TRUOC khi add Pool IPs
  * Nhieu IP tren interface → nhieu route conflict → OS chon sai route
  * Chi giu Pool IPs (keep_ips), xoa tat ca IP khac co cung prefix
- Log target host:port khi connect fail (3 lan dau) de debug
- File: modules/proxy_providers/ipv6_provider.py, modules/ipv6_proxy.py, server/chrome_pool.py

1.0.610
2026-03-30 (VN)
Fix: Server IPv6 Pool - interface setup giong ipv6_rotator

- _add_to_interface() viet lai giong logic ipv6_rotator.set_ipv6():
  * CLEANUP old ::/0 routes truoc (nhieu default routes → routing loan)
  * Firewall ICMPv6 NDP TRUOC khi add address
  * IPv6 prefix policy (prefer IPv6 over IPv4)
  * Wait 8s cho NDP (truoc chi 3s)
  * DNS chay rieng (non-fatal)
  * VERIFY IP tren interface sau setup
  * Log chi tiet tung command, ping gateway check Reply
- File: modules/proxy_providers/ipv6_provider.py

1.0.609
2026-03-30 (VN)
Fix: Server Chrome workers khong co mang khi dung IPv6 Pool

- BUG 1: Pool IP bi ghi de boi GUI IPv6 list → Fix: skip khi pool mode
- BUG 2: setup_dedicated() khong add IPv6 to interface → Fix: netsh add
- BUG 3: netsh timeout kills sequence → Fix: try/except rieng
- BUG 4: 403 rotation Pool+ProxyProvider → Fix: rotate_to()
- File: modules/proxy_providers/ipv6_provider.py, server/chrome_pool.py, server/app.py

1.0.608
2026-03-30 (VN)
Change: IPv6 Pool FIFO queue - tranh trung IP khi cap phat

- TRUOC: get_ip() lay IP available DAU TIEN trong list (vi tri co dinh)
  * Release IP → quay lai vi tri cu → duoc cap lai NGAY cho VM tiep theo
  * Pool restart → IP thu tu giong nhau → de trung voi VM dang dung
- SAU: FIFO queue - IP duoc cap xong → chuyen ve CUOI list
  * get_ip(): Lay tu DAU list, chuyen entry ve CUOI
  * release_ip(): Tra IP → chuyen ve CUOI (cap lai sau cung)
  * IP lau nhat khong dung → o DAU → duoc cap truoc
  * IP vua dung xong → o CUOI → cap lai cuoi cung
  * Du pool restart, thu tu van dung (luu trong pool.json)
- HIEU QUA: Giam trung IP giua cac VM, dac biet khi pool restart/reset
- File: ipv6/ipv6_pool.py - get_ip(), release_ip()

1.0.607
2026-03-30 (VN)
Change: Server GUI - Gop IPv6 toggle + Proxy Provider thanh 1 selector ro rang

- TRUOC: 3 thanh phan rieng biet (IPv6 toggle, IPv6 list, Pool API URL, Proxy Provider combo)
  * User phai hieu cach ket hop chung → de nham
- SAU: 1 dropdown "Che do IPv6" voi 4 lua chon:
  * Tat: Khong IPv6, khong proxy
  * IPv6 Pool: Nhap Pool API URL → tu dong lay/rotate IPv6 (KHUYẾN DÙNG)
  * IPv6 Thu cong: Nhap danh sach IPv6 thu cong
  * Webshare: Dung Webshare.io rotating proxy
- Moi che do chi hien settings lien quan (an phan khong can)
- Them nut TEST KET NOI cho IPv6 Pool (goi /api/status, hien so IP)
- Backward compat: Migrate settings cu tu ipv6_toggle + proxy_type sang ipv6_mode
- File: server/server_gui.py

1.0.606
2026-03-30 (VN)
Change: FIX server chrome_pool.py - Pool API tra ve dict thay vi string

- BUG: Pool API tra ve dict {"ip": "...", "gateway": "..."} nhung code xu ly nhu string
  * ipv6[:30] tren dict → TypeError: unhashable type: 'slice'
  * rotate_pool_ip() ket qua cung la dict → crash khi so sanh
- FIX: Them isinstance(pool_result, dict) check o 2 cho:
  * init_workers(): Extract "ip" tu dict truoc khi gan ipv6
  * 403 rotation: Extract "ip" tu rotate result truoc khi so sanh
- File: server/chrome_pool.py

1.0.605
2026-03-30 (VN)
Change: IPv6 rotator register IP truoc khi get_ip moi

- VAN DE: Pool chay SAU VM → VM da co IPv6 tren interface
  * _init_via_pool() luon goi get_ip → lay IP MOI (bo IP cu)
  * 8 VM × 2 setup (login + API) = 16 get_ip → chiem 16 IP thay vi 8
  * IP cu van nam tren interface nhung pool khong biet
- FIX: _init_via_pool() thu register IP hien tai truoc
  * get_current_ipv6() → lay IP tren interface
  * register_ip(current_ip) → pool danh dau in_use (match subnet)
  * Chi get_ip moi neu register fail hoac no connectivity
  * Giam so IP bi chiem tu 16 → 8
- File: modules/ipv6_rotator.py - _init_via_pool()

1.0.604
2026-03-30 (VN)
Change: FIX IPv6 cu tich tu tren interface - xoa TAT CA IP co prefix pool truoc khi set moi

- BUG: Khi doi IPv6, chi xoa IP dang track (current_ip) va IP trong file
  * IP tu session truoc (pool) KHONG bi xoa → van nam tren interface
  * Windows co the gui traffic qua IP cu (khong co gateway) → timeout
  * Nhieu default routes tich tu → routing loan → WinError 10060
- FIX: Truoc khi set IP moi, scan TAT CA IPv6 tren interface
  * Tim IP co prefix pool (vd: 2001:ee0:b004:30xx)
  * Xoa TAT CA ngoai tru IP moi dang set
  * Interface luon chi co 1 IPv6 pool → routing sach
- File: modules/ipv6_rotator.py - set_ipv6()

1.0.603
2026-03-30 (VN)
Change: Them register_ip - VM dang ky IPv6 dang dung voi pool

- VAN DE: Pool chay SAU VM hoac RESET → khong biet VM nao dung IP nao
  * Pool cho IP da co VM dung → xung dot
  * VM dung IP cu ma pool danh dau "available" → 2 VM cung subnet
- GIAI PHAP: Them /api/register_ip
  * VM khoi dong → check interface co IPv6 cu khong
  * Neu co → goi register_ip (match theo subnet) → pool danh dau in_use
  * Neu khong co hoac register fail → goi get_ip binh thuong
- FLOW MOI:
  * VM start → _get_existing_ipv6() doc IPv6 tu netsh
  * → POST /api/register_ip {ip, worker}
  * → Pool match subnet → danh dau in_use → tra IP + gateway
  * → VM dung IP tu pool (co the khac worker IP cu nhung cung subnet)
- Files: ipv6/ipv6_pool.py, ipv6/ipv6_server.py,
         modules/ipv6_pool_client.py, modules/proxy_providers/ipv6_pool_provider.py

1.0.602
2026-03-30 (VN)
Change: FIX refill tao gateway moi + auto-adjust max_pool_size

- BUG 1: _refill() chi tim gateway CO SAN tren router
  * Neu gateway bi xoa (do burn cu) → "No more subnets available"
  * Pool bi ket o 154 du range co 155 subnets
- FIX 1: Khi khong co gateway co san, TU DONG TAO gateway moi tren router
- BUG 2: max_pool_size mac dinh = 20, pool co 155 IP
  * _refill() check active_count >= 20 → "Max pool size reached" → khong them IP
- FIX 2: Auto-adjust max_pool_size = len(pool) + 10 khi load tu router
- File: ipv6/ipv6_pool.py - _refill(), initialize()

1.0.601
2026-03-30 (VN)
Change: FIX burn_ip het subnet - dung lai subnet cu voi worker IP random moi

- BUG: Pool 155 IP dung het 155 subnets (range 101-255)
  * Burn xoa 1 subnet + loai tru subnet cu = 155/155 da dung → "het subnet moi!"
  * Pool mat 1 IP moi lan burn (155 → 154 → 153...)
- FIX: Khi het subnet moi, cho phep dung lai subnet cu
  * Gateway cu da bi xoa → tao lai gateway moi cho cung subnet
  * Worker IP se duoc random moi hoan toan → Google khong nhan ra
  * Pool luon giu du so luong IP
- File: ipv6/ipv6_pool.py - burn_ip()

1.0.600
2026-03-30 (VN)
Change: Redesign IPv6 Pool GUI - auto start + dark theme + workers panel

- AUTO: Mo tool = tu dong ket noi router + start API server (khong can bam gi)
- DARK THEME: Giao dien toi, de nhin, nhat quan voi cac GUI khac
- 4 O LON: San sang / Dang dung / Da burn / Tong pool - so lieu lon de doc
- WORKERS PANEL: Hien thi may nao dang dung IP nao (ten + IP + so lan get/rotate/burn)
- API STATS: Lay / Doi / Burn / Tra - 1 dong gon
- Them cot Gateway trong danh sach IP
- Don gian nut: chi giu DOI TAT CA / RESET / Them IP / Test IP / Burn / Doi
- Config dialog rieng (khong chiem cho tren giao dien chinh)
- Auto refresh 5 giay
- File: ipv6/ipv6_gui.py

1.0.599
2026-03-30 (VN)
Change: Burn IP 403 = xoa gateway cu + tao gateway moi (giong rotate_all cho 1 IP)

- TRUOC: Burn chi danh dau "burned" trong tracking, gateway van con tren router
  * IP bi 403 van chiem slot tren router → lang phi subnet
  * Phai RESET thu cong khi het IP
- SAU: Burn = xoa gateway cu tren router + tao gateway moi voi subnet RANDOM
  * Flow: burn → xoa ::1/64 cu → chon subnet moi → tao ::1/64 moi → IP moi available
  * Tu dong thay the, khong can RESET
  * Pool luon co du IP available
- File: ipv6/ipv6_pool.py - burn_ip()

1.0.598
2026-03-30 (VN)
Change: FIX 2 van de: netsh timeout/routes tich tu + Chrome mode sai

- BUG 1: netsh timeout 5s qua ngan → routes ::/0 tich tu → routing loan
  * Delete route timeout → route cu van con → them route moi → 3-5 routes
  * Windows gui packet di gateway sai → trang load cham/dut
- FIX 1:
  * CLEANUP truoc: xoa TAT CA routes ::/0 cu truoc khi them moi
  * Tang timeout: 5s → 20s (retry 30s neu fail)
  * Tang get_current_ipv6 timeout: 10s → 20s
  * Tang verify timeout: 5s → 15s
  * Canh bao khi co >1 default route
- BUG 2: Chrome dung LOCAL SERVER mode du generation_mode=api
  * smart_engine.py chi check local_server_enabled, khong check generation_mode
  * v1.0.589 chi fix cho thumbnail, chua fix cho generate_images_api
- FIX 2: Them check generation_mode != 'api' cho generate_images_api
- Files: modules/ipv6_rotator.py, modules/smart_engine.py

1.0.597
2026-03-30 (VN)
Change: Tang proxy buffer 64KB → 256KB - tai anh/video nhanh hon

- recv buffer 64KB → 262144 (256KB)
- Anh 500KB: 8 lan doc → 2 lan doc
- Files: modules/ipv6_proxy.py, modules/proxy_providers/webshare_provider.py

1.0.596
2026-03-30 (VN)
Change: FIX proxy load cham/dut giua chung - tang buffer + timeout + TCP_NODELAY

- BUG: Trang load cham hoac bi dut giua chung khi dung IPv6 pool
- NGUYEN NHAN:
  * Buffer 8KB qua nho → nhieu round-trip khi tai anh lon
  * send() chi gui 1 phan data → mat du lieu giua chung
  * select timeout 60s → Google Flow xu ly 2-3 phut → proxy ngat ket noi
  * Khong co TCP_NODELAY → Nagle's algorithm them ~40ms delay/packet
- FIX:
  * Buffer 8KB → 64KB (giam 8x so round-trip)
  * send() → sendall() (dam bao gui TOAN BO data)
  * select timeout 60s → 300s (du cho Google Flow xu ly)
  * Them TCP_NODELAY cho ca client + remote socket
- Files: modules/ipv6_proxy.py, modules/proxy_providers/webshare_provider.py

1.0.595
2026-03-30 (VN)
Change: Bo sung day du settings trong Dialog - giong VM settings.yaml

- Them 20+ settings con thieu: video_model, video_aspect_ratio, flow_delay,
  flow_timeout, ken_burns_intensity, max_parallel_api, min/max_scene_duration,
  browser_headless, browser_delay_between_prompts, parallel_browsers, v.v.
- Nhom theo chuc nang: CHE DO CHAY / ANH / VIDEO / BROWSER / GIOI HAN / PROXY
- Them section header mau xanh de de phan biet
- Scrollable khi man hinh nho
- File: control/master_control.py

1.0.594
2026-03-30 (VN)
Change: Don gian hoa Settings Dialog trong Master Control

- Bo checkbox phuc tap, dung "--" lam gia tri mac dinh (= khong sync)
- Grid layout don gian thay vi scrollable canvas
- Combo box: chon gia tri = master kiem soat, de "--" = VM giu local
- Cua so nho hon, de su dung hon
- File: control/master_control.py

1.0.593
2026-03-30 (VN)
Change: Them control/ vao danh sach cap nhat GitHub ZIP

- BUG: control/master_control.py KHONG duoc copy khi update tu GitHub
- FIX: Them copy control/*.py trong ca 3 updater:
  * vm_manager.py (watchdog update)
  * vm_manager_gui.py (GUI update button)
  * server/server_gui.py (server GUI update)
- Files: vm_manager.py, vm_manager_gui.py, server/server_gui.py

1.0.592
2026-03-30 (VN)
Change: Master Settings Control - dieu khien settings VM tu xa

- Master Control GUI: Them nut SETTINGS mo dialog chinh settings
- Dialog cho phep:
  * Chon target: GLOBAL (tat ca VM) hoac tung VM rieng
  * Tick checkbox = master kiem soat key do, bo tick = VM giu local
  * Cac settings: generation_mode, chrome_model_index, topic, video_mode...
  * Luu/Xoa settings
- VM tu dong sync moi 60s tu control/settings/ tren shared drive
- Cau truc:
  * control/settings/_global.yaml → tat ca VM
  * control/settings/KA4-T3.yaml → rieng VM KA4-T3 (override global)
- Whitelist: Chi sync cac key an toan (khong ghi de API keys, paths)
- Files: control/master_control.py, modules/settings_sync.py, vm_manager.py

1.0.591
2026-03-30 (VN)
Change: Tang retry vao project 3→5 + F5 refresh 3 lan moi page load

- Khi mang cham, page load 1 nua roi dung → Chrome khong thay textarea
- FIX 1: Tang max_nav_retries tu 3 len 5 (nhieu co hoi hon)
- FIX 2: Sau khi navigate, F5 refresh 3 lan (moi lan cach 1s)
  * F5 giup page load tiep phan con thieu
  * Doi 2s sau F5 cuoi de page on dinh
  * Roi moi bat dau dem thoi gian cho textarea
- File: modules/drission_flow_api.py

1.0.589
2026-03-30 (VN)
Change: FIX Thumbnail chay sai mode - ton trong generation_mode setting

- BUG: Thumbnail luon dung LOCAL SERVER MODE khi local_server_enabled=true
  * Bo qua generation_mode setting → Chrome 2 chay server mode du chon API
  * "Khong co bearer_token" → thumbnail fail
- FIX: Chi dung server mode khi generation_mode KHONG phai 'api'
  * generation_mode=api → thumbnail dung API (giong Chrome 1)
  * generation_mode=server/chrome → thumbnail dung server
- File: modules/smart_engine.py

1.0.588
2026-03-30 (VN)
Change: Chan WebRTC leak IPv4 trong Chrome

- Chrome co the lo IPv4 qua WebRTC du dung proxy
- Them flag: --force-webrtc-ip-handling-policy=disable_non_proxied_udp
- Ket hop v1.0.587 (proxy khong fallback IPv4) → Chrome CHI dung IPv6
- File: modules/drission_flow_api.py

1.0.587
2026-03-30 (VN)
Change: SOCKS proxy chi dung IPv6 - bo IPv4 fallback

- Truoc (v1.0.572): Proxy fallback IPv4 khi target khong co AAAA record
  * IP checker thay ca IPv6 va IPv4 → user thay Chrome van dung IPv4
- Sau: Proxy CHI ket noi qua IPv6, KHONG fallback IPv4
  * Chrome chi thay IPv6 (IP pool)
  * RDP van hoat dong binh thuong (khong qua proxy)
- File: modules/ipv6_proxy.py - _connect_via_ipv6()

1.0.586
2026-03-30 (VN)
Change: FIX ROOT CAUSE - Windows Firewall chan ICMPv6 → NDP fail → gateway unreachable

- ROOT CAUSE: Windows Firewall tren VM KHONG co rule cho ICMPv6
  * MikroTik gui NDP (Neighbor Solicitation) hoi "ai la 3075::1?" → VM khong tra loi
  * MikroTik neighbor table: VM address = "failed"
  * Ket qua: gateway UNREACHABLE → IPv6 khong hoat dong → Chrome dung IPv4
- FIX: Tu dong them firewall rules cho ICMPv6 khi set_ipv6()
  * "ICMPv6-NDP-In" - cho phep NDP vao (MikroTik tim VM)
  * "ICMPv6-NDP-Out" - cho phep NDP ra (VM tim gateway)
  * Chi chay 1 lan dau (flag _icmpv6_rule_added)
- THEM: on-link route /64 cho subnet (v1.0.585)
  * Windows can biet gateway nam cung mang de gui packet
- File: modules/ipv6_rotator.py

1.0.584
2026-03-30 (VN)
Change: FIX Chrome dung IPv4 - gateway ping chay SAU adapter wait + fix leading zeros

- BUG: Gateway ping chay TRUOC khi doi adapter (5s) → NDP chua hoan thanh → LUON fail
  * test_ipv6_connectivity() chay sau set_ipv6() cung co the fail
  * IPv6Provider setup fail → Chrome mo KHONG CO proxy → dung IPv4
  * User thay IPv4 khi check IP
- FIX 1: Di chuyen gateway ping + address check tu TRUOC → SAU time.sleep()
  * Truoc: netsh commands → ping gateway (FAIL) → DNS → sleep 5s → verify
  * Sau: netsh commands → DNS → sleep 8s → ping gateway (OK) → verify
  * Tang adapter wait 5s → 8s de NDP kip hoan thanh
  * Gateway ping co timeout 5s (du cho NDP)
- FIX 2: _get_gateway_for_ipv6() van dung exploded format (leading zeros)
  * exploded: "2001:0ee0:b004:3075::1" (SAI - co 0ee0)
  * compressed: "2001:ee0:b004:3075::1" (DUNG)
  * Doi sang ipaddress.IPv6Network + str() (giong ipv6_pool.py)
- FIX 3: Show DAD State trong address debug log
- File: modules/ipv6_rotator.py

1.0.583
2026-03-30 (VN)
Change: FIX DAD conflict - Worker IP chi tren VM, KHONG tren MikroTik

- ROOT CAUSE: rotate_all() tao worker IP tren MikroTik + VM cung add cung IP
  * Windows DAD (Duplicate Address Detection) phat hien trung → "DAD State: Duplicate"
  * IP bi danh dau Duplicate → KHONG dung duoc → no connectivity
  * Day la ly do CHINH khien IPv6 pool khong hoat dong!
- FIX: MikroTik CHI co gateway ::1/64 (de routing). Worker IP CHI tren VM.
  * rotate_all(): Chi tao gateway, KHONG tao worker IP tren router
  * _load_all_from_router(): Load gateway → generate random worker IP LOCAL
  * _refill(): Tim gateway subnets, generate worker IP LOCAL
  * _sync_with_router(): Check gateway ton tai (khong check worker IP)
- Flow moi:
  * MikroTik: 155 gateway ::1/64 tren bridge (connected route cho moi /64)
  * VM request IP → Pool generate random IP → VM add vao interface
  * Chi VM co IP → DAD pass → "Preferred" → connectivity OK
- File: ipv6/ipv6_pool.py

1.0.582
2026-03-30 (VN)
Change: DEBUG IPv6 - fix verify command + show addresses + route table

- v1.0.581 verify dung "show route ::/0" → "The parameter is incorrect" (netsh khong ho tro filter)
- FIX: Dung "show route" (khong filter) roi grep "::/0" trong output
- THEM: Show IPv6 addresses tren interface (xac nhan IP da duoc add dung)
- THEM: Show route table context khi default route missing
- Log netsh command errors (rc + stderr) khi fail
- Ping gateway test NDP reachability
- File: modules/ipv6_rotator.py - set_ipv6()

1.0.580
2026-03-30 (VN)
Change: FIX gateway format - dung compressed IPv6 (khong co leading zeros)

- BUG: _get_gateway_for_ip() dung ipaddress.IPv6Address.exploded → tra ve "2001:0ee0:b004:3098::1"
  * Leading zeros (0ee0 thay vi ee0) co the khong tuong thich voi Windows netsh
  * VM nhan gateway sai format → khong add route duoc → no connectivity
- FIX: Dung ipaddress.IPv6Network + str() cho compressed format
  * str(gateway) tra ve "2001:ee0:b004:3098::1" (khong co leading zeros)
  * Tuong thich 100% voi Windows netsh va moi OS
- File: ipv6/ipv6_pool.py - _get_gateway_for_ip()
